mod Pl04
  type Nat = Zero | Suc(Nat)
  type MyList(a) = LNil | LCons(a, MyList(a))
  fn one() -> MyList(Nat) = LCons(Zero(), LNil())
  fn hd(d: Nat, l: MyList(Nat)) -> Nat = match l
    LCons(x, xs) -> x
    LNil() -> d
  fn g() -> Nat = hd(Suc(Zero()), one())
end
