mod Imp02
  type Nat = Z | S(Nat)
  type Vec(a) indices (n: Nat)
    vnil : Vec(a, Z)
    vcons : (k: Nat) -> a -> Vec(a, k) -> Vec(a, S(k))
  fn onlyNil(v: Vec(Nat, Z)) -> Nat = match v
    vnil() -> Z()
end
