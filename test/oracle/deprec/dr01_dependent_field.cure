mod Dr01
  type Nat = Z | S(Nat)
  type SNat indices (n: Nat)
    szero : SNat(Z)
    ssuc : SNat(n) -> SNat(S(n))
  rec DPair
    fst: Nat
    snd: SNat(fst)
  fn mk() -> DPair = DPair{fst: S(Z()), snd: ssuc(szero())}
  fn getFst(p: DPair) -> Nat = p.fst
end
