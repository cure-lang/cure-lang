mod Whnf04
  type Nat = Z | S(Nat)
  fn plus(a: Nat, b: Nat) -> Nat = match a
    Z() -> b
    S(k) -> S(plus(k, b))
  type Vec indices (n: Nat)
    vz : Vec(Z)
    vs : Vec(k) -> Vec(S(k))
  fn wants(v: Vec(plus(Z, S(Z)))) -> Nat = Z()
  fn use() -> Nat = wants(vz())
end
