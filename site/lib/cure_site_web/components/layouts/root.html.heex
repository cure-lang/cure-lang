<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta
      name="description"
      content="Dependently typed programming for the BEAM through one kernel-checked compiler pipeline."
    />
    <.live_title default="Cure" suffix=" — Dependently-Typed BEAM Language">
      {assigns[:page_title]}
    </.live_title>

    <%!-- OpenGraph / Twitter card metadata for link previews --%>
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Cure — Dependently-Typed BEAM Language" />
    <meta
      property="og:title"
      content={(assigns[:page_title] || "Cure") <> " — Dependently-Typed BEAM Language"}
    />
    <meta
      property="og:description"
      content="Dependently typed programming for the BEAM through one kernel-checked compiler pipeline."
    />
    <meta property="og:image" content={url(~p"/images/logo-128x128-nobg.png")} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="128" />
    <meta property="og:image:height" content="128" />
    <meta property="og:image:alt" content="Cure language logo" />
    <meta property="og:url" content={Phoenix.Controller.current_url(@conn)} />
    <meta name="twitter:card" content="summary" />
    <meta
      name="twitter:title"
      content={(assigns[:page_title] || "Cure") <> " — Dependently-Typed BEAM Language"}
    />
    <meta
      name="twitter:description"
      content="Dependently typed programming for the BEAM through one kernel-checked compiler pipeline."
    />
    <meta name="twitter:image" content={url(~p"/images/logo-128x128-nobg.png")} />
    <meta name="twitter:image:alt" content="Cure language logo" />

    <%!-- Sitemap discovery for crawlers that honour the link tag --%>
    <link rel="sitemap" type="application/xml" title="Sitemap" href={~p"/sitemap.xml"} />

    <%!-- Schema.org / JSON-LD structured data. The baseline
         WebSite + Organization records are always emitted; route-
         specific schemas (TechArticle, APIReference, HowTo, ...) are
         opted in via `assigns[:json_ld]`. --%>
    <%= for json <- CureSiteWeb.JsonLd.entries(assigns) do %>
      <script type="application/ld+json">
        <%= Phoenix.HTML.raw(json) %>
      </script>
    <% end %>

    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <Yeesh.Live.TerminalComponent.xterm_style />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        document.addEventListener("DOMContentLoaded", () => {
          document
            .querySelectorAll("button[data-phx-theme]")
            .forEach((el) => {
              el.addEventListener("click", () => {
                setTheme(el.dataset.phxTheme)
              })
            })
        })
      })();
    </script>
  </head>
  <body class="min-h-screen">
    {@inner_content}
  </body>
</html>
