## Transparent actor example with a typed integer payload.

use Std.Actor

actor Worker
  state Int
  initial 0
  messages Atom
  handle_cast
    pickup
      message == :inc -> %[:noreply, state + 1]
      message == :reset -> %[:noreply, 0]
      else -> %[:noreply, state]
