## Transparent supervisor example. Child-spec algebra is defined in Cure.

use Std.Supervisor

sup Colony
  children
    worker Worker as worker_a
    worker Worker as worker_b
    worker Echo as echo
