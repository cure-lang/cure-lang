## Transparent queue actor with a typed atom-list state.

use Std.Actor

actor Queue
  state List(Atom)
  initial assert_type [:empty] : List(Atom)
  messages Atom
  handle_cast
    pickup
      message == :clear -> %[:noreply, assert_type [] : List(Atom)]
      else -> %[:noreply, [message | state]]
