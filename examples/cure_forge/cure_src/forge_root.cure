## Transparent supervisor example.

use Std.Supervisor

sup Forge.Root
  children
    worker Main.Metrics as metrics
    worker Main.Logger as logger
    worker Main.Queue as queue
    worker Main.Pool as pool
