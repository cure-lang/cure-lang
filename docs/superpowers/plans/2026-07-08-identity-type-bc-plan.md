# Identity-Type Phases B/C Implementation Plan (rewrite → case; retire primitives)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans, composed with the cure-porting skill's loop and constraints. Steps use checkbox syntax.

**Goal:** Retire the primitive `{:rewrite}` transport (Phase B: every producer emits a single-branch inductive `:case` instead, via in-branch re-elaboration) and then strip the dead `{:eq}`/`{:refl}`/`{:veq}`/`{:rewrite}` Core forms (Phase C), flipping the validator to `:reject`, under the full TCB gate.

**Spec:** `docs/superpowers/specs/antigen/2026-07-04-identity-type-as-inductive.md` — read the "Current state — REVISED 2026-07-08 (evening)" section FIRST; it is the authoritative re-scope (K6 gate stale; params-on-spine landed `b355753` kernel / `f3b0e73` elaborator; B′ stdlib already done; the real problem is computed-endpoint desugaring, diagnosed in `c635e8c`'s revert message — read that commit message in full with `git show c635e8c --format="%B" -s`).

## Global Constraints (cure-porting discipline — non-negotiable)

- **Two pipelines:** ALL work in `lib/cure/elab/*` (Phase B) and `lib/cure/core/*` (Phase C). `lib/cure/compiler/*` and `lib/cure/types/*` are decoys — never edit them for this.
- **Ghost commits:** `--author="Made In Heaven <madeinheaven@madeinheaven.com>"`, no trailers. **Explicit pathspec staging only** (`git add -- <path>`), never `-A`.
- **One build at a time.** Scoped `mix test <file>` while iterating; `mix cure.oracle <cluster>` for verdicts (never hand-write a verdict); full suite ONCE, alone, at each gate. Oracle replay (`mix test test/oracle_replay_test.exs`) green before every commit.
- **`mix cure.oracle <cluster>` is DESTRUCTIVE, not a read-only check** (verified `lib/mix/tasks/cure.oracle.ex:73`): it unconditionally OVERWRITES the tracked `test/oracle/<cluster>/verdicts.json` with the live run's result every time it's invoked, regardless of whether the live verdict matches the prior frozen one (a mismatch only prints `<-- TRIAGE`; it does not fail the task or skip the write). The actual pass/fail signal comes ONLY from the subsequent `mix test test/oracle_replay_test.exs`. Consequence for the "immutable frozen oracle verdicts" constraint below: if a pin step reveals drift (replay fails, or a `<-- TRIAGE` line appears), the on-disk fixture has ALREADY been mutated — `git checkout -- test/oracle/<cluster>/verdicts.json` to restore the frozen value BEFORE investigating/retrying, so a reverted attempt can never leave a silently-changed "frozen" fixture sitting in the worktree for a later pathspec-staged commit to accidentally pick up.
- **TCB (Phase C) is HARD-STOP-and-review:** red-green + antibody extension + full Antigen + full suite. If any Phase-C removal changes a VERDICT anywhere, stop — that means a primitive was not actually dead.
- **Tests and frozen oracle verdicts are immutable.** The behavioural pin is byte-identical verdicts across: `rewrite/rw01-rw09`, `refl/rf01-rf05`, `frp/*` (sentinel: `frp01_par_assoc` — computed endpoints, the case that killed `d44edb8`), `with/*` + `withmulti/*` (sentinel: `wi05_sibling_refine` — the two producer sites in with-transport machinery), `cycle/*`, `dotpat/*`.
- Worktree: `/Users/ch/Develop/esp32-beam/cure-lang/.claude/worktrees/kernel-parity-batch`, branch `autopilot/kernel-parity-batch`.

## Ground truth (hardened-spec-verified; re-verify line numbers before editing)

- **Seven `{:rewrite, …}` producer sites** in `lib/cure/elab/elaborator.ex`: `1083`, `1095`, `1162`, `1166` (bridge_step outer wrap), `1195`, `1856` (`elaborate_with_eq_branch`), `3421` (`elaborate_carried_eq_branch`).
- The kernel `{:rewrite}` rule (`kernel.ex:~117`) infers the proof; after Phase B nothing reaches it. `ensure_eq`/`eq_parts` currently accept both `{:veq}` and `{:vdata, :Equivalent}`.
- `bridge_step` is dormant in the current corpus (traced); its migration must still be correct but has no live oracle coverage — Phase B adds a unit fixture for it.
- `rw_case_build` (the reverted naive attempt) shifted a pre-elaborated body +1 into the branch — WRONG for computed endpoints. The correct construction re-elaborates the body inside the refl branch, routed through `elaborate_match`'s branch machinery (`build_motive` sentinels handle computed indices).
- Validator (verified `validator.ex:42-85`): both `no_eq_node` and `no_rewrite_node` default to `:warn` in `@wave0_config` (`:45-46`, dev-time). `@release_config` (`:78-81`) is **not symmetric**: `no_eq_node` is ALREADY `:reject` there (landed K1a, pre-dates this batch) but `no_rewrite_node` is deliberately absent from `@release_config`'s overrides, so it stays at the Wave-0 `:warn` even at release — the code comment at `:76-77` says so explicitly ("stays `:warn` until Phase B... retires the still-produced transport eliminator"). Task C1 Step 4 (the validator flip) must therefore (a) add `no_rewrite_node: :reject` to `@release_config` (new), and (b) flip the *Wave-0/dev-time default* `no_eq_node` from `:warn` to `:reject` in `@wave0_config` (release already has it) — two different edits, not one symmetric flip.
- Antigen antibody exists: `test/antigen/eq_inductive_antibody_test.exs` — but 3 of its 4 tests (`"rewrite over an inductive Equivalent(Nat,Z,S Z)..."`, `"a rewrite whose proof has a same-shaped non-Eq family..."`, and job 3 of the termination test) directly construct raw `{:rewrite, …}` Core nodes and assert `Kernel.infer` returns `{:ok, …}` on them. Task C1's own red test (Step 1) demands the opposite post-retirement (`{:rewrite,…}` becomes an unknown form). These three tests are IN DIRECT CONFLICT with Task C1's goal as currently written and MUST be handled as part of Task C1, not left for someone to discover later: a `:case`-based replacement asserting the identical soundness property is ADDED (and cross-checked against the original) in Step 2, and the original `{:rewrite}`-constructing test is deleted only in Step 3's removal commit, as a documented consequence of that commit — never a silent or premature drop. See Task C1 below.
- `test/cure/core/validator_test.exs`'s `"a legacy node in the declared TYPE is caught too, not just the body"` test (`:142-161`) also hand-constructs a raw `{:eq, …}` type and `{:refl, …}` body specifically to prove the validator's `no_eq_node: :reject` override catches it. Once C1 retires the `{:eq}/{:refl}` kernel clauses, this construction can no longer reach the validator's clause-scan the way the test currently expects (the kernel's `infer`/`check` would reject it earlier, as an unknown form, never reaching `Kernel.check_def`'s validator pass in the same shape) — this test needs the same treatment as the antibody tests above.
- **Antigen's own generator/shrink infrastructure is a heavy OUTSIDE-`elaborator.ex` producer of the primitive forms**, missed by Task C1's file list: `lib/antigen/generators/equality.ex` (whole file — generates `{:refl, a}`/`{:eq, ty, a, b}`/`{:rewrite, …}` as its entire purpose), `lib/antigen/generators/rewrite.ex` (whole file — `{:eq,…}`/`{:refl,…}` rewrite-soundness challenges), `lib/antigen/generators/dep_match.ex`, `indexed.ex`, `positivity.ex`, `malformed.ex`, `mutation.ex`, `conv_pair.ex` (scattered `{:eq,}`/`{:refl,}`/`{:rewrite,}` literals), plus `lib/antigen/shrink.ex:173-176` and `lib/antigen/assays/totality_closure_assay.ex:97-98`. These feed primitive-form terms straight into `Kernel.infer`/`check` as part of the property-based soundness suite; once C1 removes those kernel clauses, these generators don't get a "verdict drift" (which Task C1's HARD-STOP language is tuned to detect) — they get outright `FunctionClauseError`/unknown-form crashes across a large slice of the Antigen corpus, which would sink the "full Antigen suite" requirement in Task C-Gate. Task C1 must include migrating these generators to build `{:data,:Equivalent,…}`/`{:ctor,:reflexive,…}` (mirroring what the elaborator itself already does) wherever they mean to construct a *valid* equality/reflexivity term, and must explicitly re-scope (with recorded justification, not silent deletion) any generator whose entire point was to exercise the now-dead primitive shape.

---

### Task B1: The desugar core — `rewrite_to_case` via in-branch re-elaboration

**Files:** `lib/cure/elab/elaborator.ex` (new helper + migrate the `rewrite_plan` family sites 1083/1095/1162/1195); test `test/cure/elab/rewrite_as_case_test.exs` (new).

**Contract produced:** a helper (suggested name `elaborate_rewrite_as_case`) that, given the proof expr, motive info, and the UNELABORATED body AST + expected type, produces `{:case, proof_core, motive, [refl_branch]}` where the refl branch's body is elaborated INSIDE the branch context (indices refined by the unifier), reusing `elaborate_match`'s machinery rather than reimplementing it. Discovery is part of this task: read `elaborate_match/6` (`elaborator.ex:1350`) and `build_motive/6` (`:1894`) FIRST — they show `elaborate_match/6` has **no motive parameter at all**: it always self-computes the motive internally from `build_motive(dname, family.indices, param_terms, idx_terms, scrut_term, result_type_term)`. So the real discovery question is NOT "can we inject `rewrite_plan`'s motive into `elaborate_match`" (there is no injection point to test) — it is "does `build_motive`'s auto-derived motive, applied to a synthesized `match proof | reflexive(w) -> body` with `result_type_term` = the rewrite's own enclosing expected type, produce an obligation equivalent to `rewrite_plan`'s targeted single-endpoint substitution". Read `builtins.ex:99-115`: `Equivalent`'s sole ctor `reflexive` binds BOTH index positions to the SAME field var `w` (`result_indices = [{:var,0},{:var,0}]`), so `build_motive`'s k=2-index abstraction, applied through the shared-witness branch, collapses occurrences of BOTH endpoints to the same fresh `w` inside the branch — this is plausibly equivalent to (and more uniform than) `rewrite_plan`'s single-target `abstract_term`, but confirm it rather than assume it: check that `elaborate_match`'s branch-context unification actually refines other in-scope uses the same way `rewrite_plan`'s manual substitution does, on both the plain (1083/1095/1195) and computed-endpoint (frp) shapes. STRONGLY PREFERRED for the sites that have a genuine surface body AST to re-elaborate: desugar `{:rewrite_expr, [proof_ast, body_ast]}` (the top-level entry at `:861`) at the SURFACE level into a synthesized single-arm match AST, so `elaborate_match` does all the work. This does NOT apply uniformly to all seven sites: `bridge_step`'s inner bridge proof (`:1162`) already has a fully-BUILT CORE proof term (`mk_refl_infer(ty_s,s_nf)` / body `mk_refl(s_nf)`), not surface AST awaiting elaboration — for that site there is nothing to "desugar at the surface level"; it must go through the internal-functions route (build the `:case` term directly, type-CHECKING the pre-built body against the branch's expected type rather than elaborating surface AST). STOP and report if the surface route's `build_motive`-derived motive is not actually equivalent to `rewrite_plan`'s substitution for the plain and computed-endpoint cases.

- [ ] **Step 1 (red):** `test/cure/elab/rewrite_as_case_test.exs` with two fixtures asserted via `Program.check_ast/1`: (a) a variable-endpoint rewrite (rw01 shape) whose emitted Core (walk `env.defs[...].body`) contains NO `{:rewrite, ...}` node; (b) a computed-endpoint rewrite (miniature `frp01_par_assoc` shape — endpoints are applications, not variables) that must ACCEPT with no `{:rewrite}` node. Both red today (bodies contain `{:rewrite}`).
- [ ] **Step 2:** Implement the helper; migrate sites 1083/1095/1195 (the main `rewrite_plan` outputs — 1083 wraps, 1195 is `symmetry_proof`'s own construction), then 1162 (`bridge_step`'s inner bridge proof — NOT the symmetry path; a pre-built Core term, so this one takes the internal-functions construction regardless of what Step 1's discovery found for the surface route). Iterate against the red file ONLY. Note: Task B2 Step 1 later re-investigates whether `bridge_step` is reachable at all and may delete the whole function if it genuinely isn't — if so, this migration of 1162 is discarded along with it. That is an acceptable, bounded cost (not a defect to re-plan around): migrating 1162 correctly here is still required in the reachable case, and Ground Truth already establishes `bridge_step` as dormant-but-intended-to-be-kept-and-tested, so deletion is the fallback, not the expected outcome.
- [ ] **Step 3 (pin):** `mix cure.oracle rewrite && mix cure.oracle refl && mix cure.oracle frp` — byte-identical verdicts (esp. `frp01_par_assoc` stays accept). Then `mix test test/oracle_replay_test.exs`.
- [ ] **Step 4:** Commit (ghost, pathspec): `feat(elab): rewrite desugars to single-branch inductive case (variable+computed endpoints)`.

### Task B2: bridge_step + with-transport sites (the last three producers)

**Files:** `lib/cure/elab/elaborator.ex` (sites 1166, 1856, 3421); extend `test/cure/elab/rewrite_as_case_test.exs`.

- [ ] **Step 1 (red):** add a unit fixture that FORCES `bridge_step` (a reducible-inner-occurrence rewrite that the `contains_a`/`contains_b` syntactic paths cannot take — derive from the rw07 commit history if needed; if genuinely unreachable, document why and delete `bridge_step` instead of migrating it — dead code migration is worse than removal, but ONLY with the tracing evidence written into the test file as a comment). Add with-transport fixtures mirroring `wi05_sibling_refine`'s shape asserting no `{:rewrite}` in emitted Core.
- [ ] **Step 2:** Migrate/remove per Step 1's finding. After this, `grep -n "{:rewrite," lib/cure/elab/elaborator.ex` shows ZERO producers (traversal clauses may remain until C).
- [ ] **Step 3 (pin):** `mix cure.oracle with && mix cure.oracle withmulti && mix cure.oracle rewrite && mix cure.oracle frp`; replay green. Scoped `mix test test/cure/elab/` green.
- [ ] **Step 4:** Commit: `feat(elab): all rewrite producers retired (bridge + with-transport)`.

### Task B-Gate: intermediate full gate

- [ ] Full `mix test` once (0 failures), `mix cure.check.examples` (44), full oracle replay. Commit any strictly-necessary fix with its own red test. Commit: `test(elab): Phase B gate — no rewrite producers, full pins green`.

### Task C1: strip dead Core forms (TCB — HARD-STOP discipline)

**Files:** `lib/cure/core/term.ex`, `eval.ex`, `kernel.ex`, `quote.ex`, `certificate.ex`, `serialize.ex`, plus elab traversal clauses (`subst`, `erase.ex`, `relevance.ex`, `resolution.ex`, `unify.ex`, `generalize`/`walk` helpers — grep `:rewrite`, `{:eq,`, `{:refl,`, `:veq` and enumerate in the report). **Also required** (verified during plan review — these construct raw `{:eq,}`/`{:refl,}`/`{:rewrite,}` Core literals and will break the moment the clauses above are removed, not "drift a verdict" but crash outright, which the HARD-STOP wording below is NOT tuned to catch): `test/antigen/eq_inductive_antibody_test.exs` (3 of its 4 tests), `test/cure/core/validator_test.exs`'s `"a legacy node in the declared TYPE is caught too"` test (`:142-161`), and the Antigen generator/shrink/assay infrastructure — `lib/antigen/generators/equality.ex`, `rewrite.ex` (both whole-file), `dep_match.ex`, `indexed.ex`, `positivity.ex`, `malformed.ex`, `mutation.ex`, `conv_pair.ex`, `lib/antigen/shrink.ex:173-176`, `lib/antigen/assays/totality_closure_assay.ex:97-98`.

- [ ] **Step 1 (red):** kernel test asserting a `{:rewrite, …}` (and `{:eq}`/`{:refl}`) term no longer round-trips: `infer` returns an unknown-form error, `Term.to_external` raises/errors, serializer rejects. Red today (they succeed).
- [ ] **Step 2 (ADD `:case`-based replacement fixtures for the legacy-form test/generator producers — do NOT touch the old fixtures yet; they still run and serve as the cross-check. MUST land before Step 3's removal, since Step 3 makes the old constructions impossible to run):**
  - `test/antigen/eq_inductive_antibody_test.exs`: ADD 3 new tests, one per existing `{:rewrite,…}`-constructing test (`"rewrite over an inductive Equivalent(Nat,Z,S Z)…"`, `"a rewrite whose proof has a same-shaped non-Eq family…"`, termination job 3), each asserting the IDENTICAL soundness property (endpoint fidelity / decoy-family rejection / termination) but built via a `:case`-based refl-match instead of a raw `{:rewrite}` node. Run both old and new side by side now, while `{:rewrite}` still round-trips, to confirm the new construction agrees with the old one on every scenario — this is the evidence that Step 3's later deletion of the old 3 tests (staged in that same removal commit, as a documented consequence of retiring the mechanism they probe, not an independent weakening) is safe.
  - `test/cure/core/validator_test.exs`'s legacy-node test (`:142-161`): same ADD-then-retire-in-Step-3 treatment — a new test rebuilding the fixture's type/body via `:case`/`{:ctor,:reflexive,…}` so it still proves the validator's clause-scan reaches a def's declared TYPE, not just its body.
  - `lib/antigen/generators/*` + `shrink.ex` + `totality_closure_assay.ex` listed above: for each generator whose PURPOSE was to build a valid equality/reflexivity/rewrite term, retarget it to `{:data,:Equivalent,…}`/`{:ctor,:reflexive,…}`/`:case` (mirroring the elaborator's own retirement). For any generator whose specific purpose WAS the now-dead primitive shape (e.g. a malformed-term negative case that only makes sense as `{:eq}`/`{:refl}`), document in-file why it's retired rather than migrated — silent deletion is not acceptable, matching the antibody file's own "STOP — do not weaken the assertion" discipline.
  - Scoped `mix test test/antigen test/cure/core/validator_test.exs` green (both old and new fixtures passing side by side — `{:rewrite}` still round-trips at this point, Step 3 hasn't run yet).
  - Commit (ghost, pathspec): `test(antigen): add inductive Equivalent/case fixtures alongside legacy Eq/refl/rewrite ones, ahead of primitive retirement`.
- [ ] **Step 3:** Remove clauses file-by-file, ONE commit per coherent group, scoped `mix test test/cure/core test/cure/elab` between groups. `ensure_eq`/`eq_parts` collapse to the `{:vdata, :Equivalent}`-only forms. The commit that removes the `{:eq}`/`{:refl}`/`{:rewrite}` kernel clauses also deletes the 3 now-uncompilable old-construction tests in `eq_inductive_antibody_test.exs` and the 1 in `validator_test.exs` that Step 2's additions superseded — call this out in that commit's message so the deletion reads as a documented consequence, not a silent drop.
- [ ] **Step 4:** Flip `no_rewrite_node` AND default `no_eq_node` to `:reject` in `validator.ex` (`no_rewrite_node` needs adding to `@release_config`; `no_eq_node`'s dev-time `@wave0_config` default needs flipping — release already has it, see Ground truth); red test first asserting the validator rejects a smuggled primitive node.
- [ ] **Step 5:** Commits per group: `refactor(core): retire {:rewrite} clauses`, `refactor(core): retire {:eq}/{:refl}/{:veq} clauses`, `feat(validator): primitive identity nodes reject by default`.

### Task C-Gate: full TCB gate

- [ ] Extend `test/antigen/eq_inductive_antibody_test.exs` per spec Gate C: (i) refl-matching discharges/refines exactly as the index unifier dictates and equates NO distinct normal forms (defeq non-collapse obligation — generate distinct-normal-form pairs and assert conversion still rejects); (ii) termination unaffected; (iii) retired nodes unreachable (reuse Task C1's red test forms). Red-first where the obligation is new.
- [ ] Sequential gate: full Antigen suite → full `mix test` → `mix cure.check.examples` → full oracle replay (`mix test test/oracle_replay_test.exs`) → `mix cure.oracle rewrite/refl/frp/with/withmulti/cycle/dotpat` live re-run, byte-identical.
- [ ] Update the parity ledger (§2 of `docs/superpowers/specs/roadmap/2026-07-02-idris-parity-roadmap.md`): the Eq/rewrite rows graduate; note K/UIP stance unchanged.
- [ ] Commit: `feat(kernel): identity type fully inductive — primitives retired (Phase B/C complete)`.

## STOP conditions (report, don't improvise)

- The surface-match route's `build_motive`-derived motive is not equivalent to `rewrite_plan`'s substitution for the plain or computed-endpoint cases (B1) — i.e. the in-branch re-elaboration cannot reproduce `rewrite_plan`'s obligation.
- Any oracle verdict drifts at any pin (esp. `frp01_par_assoc`, `wi05_sibling_refine`).
- A Phase-C removal turns out to be load-bearing (verdict change / suite failure not explained by the red tests).
- Anything requires touching `lib/cure/compiler/*` or `lib/cure/types/*`.
