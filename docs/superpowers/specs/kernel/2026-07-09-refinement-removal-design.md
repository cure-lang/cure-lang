# Refinement-types removal (task #19) — Design

## §0 Decision record

Operator order (2026-07-09, post-parity teardown batch): remove refinement types ENTIRELY for now; they return only if/when SMTCoq-style proof reconstruction is ever ported (operator 2026-07-09: a STRETCH GOAL, deliberately NOT queued as a task — nothing plans for or conditions on it). This supersedes the operational "Z3 refinement checking always present" stance for the SOLVER layer only — the **z3 binary dependency itself stays**, because GuardLint (the dependent pipeline's guard-coverage lint) is NOT refinement machinery and keeps using `Cure.SMT.Process`. The SMT trust-boundary decision (Z3 out of the TCB) is unchanged and is exactly what makes this removal kernel-silent.

**Scout-verified foundation:** refinement types are a CLASSIC-pipeline-only feature. The dependent pipeline already treats them as nonexistent — `program.ex:403-406` drops `refinement: true` type_annotations, `declarations.ex:233` never matches the 3-element refinement alias parts, `program.ex:233` excludes `Std.Refine` from the dependent auto-prelude. **Zero changes to `lib/cure/core/*` or `lib/cure/elab/*`** (one doctor.ex string aside) — the TCB is untouched by construction.

**Coordination with task #18 (classic-pathway work):** #19 OWNS all refinement machinery wherever it lives, including the classic-side files; #18 (whatever its final scope) must not double-delete. Sequencing: #17 (lean bridge) → #19 (this) → #18.

## §1 Cut line — DELETE

**Parser grammar (the shared front end — the one non-classic code change):**
- `parser.ex:3372-3387` `parse_refinement_type/1`; `parser.ex:3036-3041` (`type Name = {x:T|p}` alias form); `parser.ex:4349-4353` (inline `{x:T|p}` in type positions). `{x: T | p}` becomes an ordinary parse error — no bespoke "removed feature" diagnostic kept in the grammar (the removal is recorded here and in the changelog line of the commit; less dead code beats a nicer message for a feature returning via a different mechanism later).

**SMT refinement-query layer (`lib/cure/smt/` split, not delete):**
- DELETE `solver.ex`, `translator.ex`, `parser.ex` (refinement-only: subtype/sat/implication queries over surface-AST predicates; parser.ex's sole consumer is solver.ex:150).
- KEEP `process.ex` IN PLACE (GuardLint's Z3 port GenServer; relocation would churn GuardLint for zero benefit).

**Classic-pipeline refinement machinery (all under `lib/cure/types/`):**
- DELETE `refinement.ex`, `guard_refinement.ex`, `path_refinement.ex`, `pattern_refinement.ex`, `dependent.ex` (classic dependent-lite VCs, SMT-discharged — refinement family).
- STRIP every `{:refinement,…}` clause / refinement field from: `checker.ex` (14 literal `{:refinement, …}` clauses plus the refinement-named helpers that pattern-match on them — `strip_refinement/1`, `discharge_refinement/3` ~2725-2750, `verify_return_refinement/7`, `verify_refinement_arg/5`, `non_refinement_or_non_numeric?/1` — and the `Cure.SMT.Solver.check_sat` call at 2181), `type.ex` (subtype/join/compatible clauses), `unify.ex` (strip clauses :103-108/:237/:261-262), `env.ex` (`refinement_assumptions`/`refinement_var_types` fields), `stdlib.ex` (the `alias Cure.Types.{Env, Refinement, Type}` at :36 drops `Refinement`; the `:refinement`-flag alias-elaboration branch at :345-346 calling `Refinement.from_type_annotation/2`; prose at :110/:180 to reword — verified: stdlib.ex has no actual "Std.Refine name-mapping" code, only a docstring example at :50), `pattern_checker.ex`, `reduce.ex`. (`core_bridge.ex:20` is a comment — update the prose.)

**Peripheral `{:refinement,…}`/SMT consumers (STRIP the refinement/SMT branches, fields, and prose from each — none of these delete a whole file):**
- `export_types/protobuf.ex:209-211` (E068 clause), `optimizer/monomorphise.ex:196/449`, `doc/extractor.ex:167/186` + `doc/html_generator.ex:500-501`, `compiler/errors.ex` E090/W091 formatters (:56-73) + E015/E018/proof-container refinement prose, `compiler/printer.ex` refinement rendering, `project/proof.ex` (drop `:refinement` from the `kind` type/moduledoc) + `proof/verifier.ex` (drop the `:refinement`-kind `verify_one/1` clause and `verify_refinement/2`; the `:smt` kind and its `verify_smt/2` clause stay — that path replays a serialized Z3 query for the surviving `:smt` proof-container obligations, unrelated to refinement subtyping), `pgo.ex`/`pgo/profile.ex` SMT-latency hooks, `bless.ex:5`/`bless/advisor.ex:15` mentions, `mix.exs:122` + `lib/cure.ex:4` "SMT-backed verification" strings.

**Stdlib + examples:**
- DELETE `lib/std/refine.cure` (group-tag discovered; no Preload entry to edit).
- DELETE pure-refinement examples: `examples/refine_predicates.cure`, `examples/path_refinement.cure`, `examples/byte_size_refinement.cure`. Two of the three have `cure.check.examples` expectation rows to delete alongside them (`lib/mix/tasks/cure.check.examples.ex:49` = `path_refinement`, `:55` = `refine_predicates`); `byte_size_refinement` has no `@expected` entry (defaults to `:compile_only`, nothing to remove there). All three get their README mentions dropped too.
- REWRITE mixed examples: `examples/dependent_types.cure` (3 top-level aliases: NonZero/Positive/Percentage, all declared-but-unreferenced), `examples/cure_motif/cure_src/motif.cure` (7 top-level aliases: Pitch/Velocity/Channel/Bpm/StepsPerBeat/BarCount/RollReps), `examples/cure_moneta/cure_src/moneta.cure` (3 top-level aliases: PositiveAmount/NonNegAmount/Rate, **plus one inline refinement type that is NOT an alias**: `fn scale(m: Money, factor: {n: Int | n > 0}) -> Money` at moneta.cure:231 — "they merely use aliases" does not hold for moneta.cure; rewrite this one to `factor: Int` with the same one-line unchecked-invariant comment, since there is no alias site to retarget). For every alias: each `type X = {v: B | p}` becomes `type X = B` with a one-line comment noting the invariant is unchecked pending SMTCoq. This is not confined to the declaration line — all three files carry scattered doc-comments making the same now-false Z3/refinement claim about these specific aliases and must be reworded together with the declarations: `dependent_types.cure:2` ("Refinement types constrain values at compile time"), `motif.cure:5` (module-doc bullet listing "Refinement types for every MIDI-domain primitive") + `:81` ("Build a Note step from a refined Pitch and Velocity"), `moneta.cure:4` ("Demonstrates: ... refinement types (PositiveAmount, Rate)") + `:46` ("Z3 proves: PositiveAmount <: NonNegAmount...") + `:237` ("validated as positive by the Rate refinement type").
  - Verification-gate caveat: only `dependent_types.cure` sits directly under `examples/` and has a `cure.check.examples` `@expected` row (`"dependent_types" => "6"`) — that row must stay byte-identical, and if it changes, STOP (the alias was load-bearing beyond typing). `cure_motif/cure_src/motif.cure` and `cure_moneta/cure_src/moneta.cure` are **not** reached by `cure.check.examples` at all: its file discovery is `Path.wildcard(Path.join(@examples_dir, "*.cure"))`, non-recursive, so nested `cure_src/` files are never globbed, and neither has an `@expected` entry regardless. Their actual regression coverage is each subproject's own `mix test` under its own `mix.exs` (`examples/cure_motif/mix.exs`, `examples/cure_moneta/mix.exs`) — add running both subprojects' `mix test` to the §3 verification gate; "check.examples byte-identical" is not a safety net for these two files and must not be cited as one.
  - `examples/cure_motif/README.md` and `examples/cure_moneta/README.md` (unlike the deleted pure-refinement examples, these are kept files) contain extensive now-stale claims — motif's README has a dedicated "### Refinement types + SMT" section and calls every MIDI-domain primitive "refinement-typed"; moneta's README states "The Z3 solver proves at compile time that `PositiveAmount <: NonNegAmount`" (:94) and references "refinement type as a parameter" (:97). Both READMEs need the refinement/Z3 claims reworded to describe the plain-alias, unchecked-pending-SMTCoq state — in scope for this task, not deferred.

**Antigen refinement/SMT surface:**
- DELETE `assays/smt_lint.ex`, `generators/smt_query.ex`, runner rows `smt/implication|unsat|witness` (`runner.ex:366-368`), the `{:refinement, :int, "x", :positive}` row in `generators/unify_problem.ex:71` + strip clause `assays/unifier.ex:131`, the two refinement surface nodes in `generators/surface_expr.ex:109-110`.

**Live documentation (`docs/` and `site/` — not in the original scan, verified now; excludes `docs/superpowers/specs/*`, `docs/superpowers/audit_categorised.md`, and `CHANGELOG.md`, which stay as historical/audit records per §4):**
- `docs/TUTORIAL.md:99` ("The `Std.Refine` stdlib module collects useful aliases:" + the walkthrough under it), `docs/STDLIB.md` (TOC entries :32/:73 + the dedicated `### Std.Refine` section at :512), `docs/PROOFS.md:48`, `docs/STDLIB_DEPENDENT_CLAIMS_AUDIT.md` (`### Std.Refine` section :90-94+), `docs/DEPENDENT_TYPES.md:95` — strip or reword every `Std.Refine`/refinement-type claim to reflect removal-pending-SMTCoq, same as the mixed-example comments.
- `site/lib/cure_site/stdlib.ex:36` — drop `"Std.Refine"` from the rendered module list (this is live code generating the public stdlib docs page, not prose).
- `site/priv/pages/type-system.md` — the heaviest hit: opens by calling refinement types a current, Z3-verified feature (:3, :7), documents `{x: Int | P(x)} <: Int` subtyping (:98), non-emptiness checking (:160), path-sensitive refinement (:204, :286-294 incl. the `Std.Refine` paragraph at :294), the E011-E020 refinement-counterexample error range (:298), and structural refinement narrowing (:398-440) — all as present-tense, working features. `site/priv/pages/getting-started.md` (:13 Z3 install rationale, :111 stdlib module list, :224 nav blurb) and `site/priv/pages/roadmap.md` (multiple retrospective mentions — lower risk since roadmap entries are already past-tense changelog style, but confirm none read as "refinement types are available today"). This is the public-facing website; shipping it unchanged means cure-lang.org keeps advertising a feature this task just deleted — in scope, not deferred.
- Widen the §3.4 grep gate accordingly: `Std.Refine` must appear nowhere under `lib/`, `examples/`, `docs/`, or `site/` except the three named historical/audit exceptions above.
- Oracle-fixture naming note (relevant to Step 0, §3.3): `test/oracle/with/wi01_goal_refine.cure`/`.idr` and `wi05_sibling_refine.cure`/`.idr` have "refine" in the filename but are Class-C with-abstraction/goal-refinement fixtures (`with g(n) ... -> ...`) — confirmed by inspection to contain no `{x: T | p}` syntax. Step 0's check must match the classic refinement-type *syntax* (or the deleted grammar rule), not grep the substring "refin", or these two legitimate fixtures will trip a false STOP.

**Tests (deleted WITH their subsystem — enumerated, not weakened):**
- Whole files: `test/cure/smt/smt_test.exs` (34), `test/cure/smt/solver_k13_test.exs` (3), `test/cure/types/guard_refinement_test.exs` (19), `test/cure/types/path_refinement_test.exs` (6), `test/cure/types/pattern_refinement_narrowing_test.exs` (18), `test/cure/types/byte_size_refinement_test.exs` (5), `test/antigen/assays/smt_lint_test.exs` (15), **and `test/cure/types/dependent_test.exs` (16 — corrected from the earlier "selective" classification: it aliases `Cure.Types.Dependent` + `Cure.SMT.Solver` throughout; its subject module is deleted whole, so the file goes whole)**.
- Selective removals (refinement cases inside shared test files), enumerated by post-survey: `test/cure/types/checker_test.exs` (9 candidates at :796/:881/:898/:988/:1001/:1014/:1032/:1054/:1071), `stdlib_test.exs` (3 at :67/:112/:133), `unify_test.exs` (2 at :102/:145), **`test/cure/compiler/parser_structural_test.exs` (1: "refinement type" at :158, parses `{x: Int | x >= 0}` and asserts `refinement: true`), `test/cure/quote_test.exs` (1: "refinement type" at :348), `test/cure/pgo_test.exs` (the "SMT translator pgo_hint" describe block at :301+, aliases the deleted `Cure.SMT.Translator`)** — the plan carries each BY NAME with the justification "tests the deleted refinement subsystem". Removal rule: a candidate is deleted ONLY if it fails post-removal or directly references a deleted module/syntax; a candidate that still passes unchanged STAYS (immutability preferred — expected for at most `checker_test.exs:898`, the "declared Int parameter survives multi-clause guard refinement" pin, which may hold trivially once narrowing is gone). `compiler/errors*_test.exs` surveyed: zero refinement test rows found (prose only, inside surviving tests — leave). No surviving test may change.
- `test/antigen/assays/normalizer_test.exs` is NOT part of the deleted subsystem and is NOT a surviving-test exemption either — it independently constructs `{:refinement, …}` 2-and-3-tuples (lines 46/78/92-93) purely as a synthetic example of "an AST shape outside `CoreBridge.to_core`'s grammar" (V1a/V1c negative-control fixtures); it never touches the real parser or `Cure.Types.Refinement`. Left as-is it survives functionally (CoreBridge never special-cased `:refinement`, so the untranslatable-fallthrough behavior is unaffected) but it trips the §3.4 zero-`{:refinement,` grep gate. Rename the fixture tag to a tag that isn't a real or historical type-system concept (e.g. `:untranslatable_probe`) throughout that file — behavior-preserving, not a "surviving test change" in the §3.6 STOP sense since no assertion or semantics moves.

## §2 KEEP — explicitly, each verified

- `lib/cure/elab/guard_lint.ex` **byte-for-byte, with ONE named exception**: the comment at :194 names `Cure.SMT.Solver.run_with_z3` (comparing GuardLint's own `trap_exit` hardening to that pipeline's lack of it) — reword that one comment to stop naming the module being deleted (describe the same trade-off without the dead reference; no code line changes). This is the only permitted edit to the file; it is a comment, not behavior, so "GuardLint behavior changes in any way" (§3.6) does not apply. Everything else stays: `lib/cure/smt/process.ex` + `test/cure/elab/guard_lint_test.exs` + `lib/antigen/generators/elab_guard_lint.ex` + runner row `elab/guard_lint` (:350). GuardLint's verdicts gate guard-chain catch-all acceptance (elaborator.ex:2524, kernel re-checks) and shadowing warnings — ordinary pattern guards, not refinements.
- `doctor.ex:103-124` DOC-ENV-Z3 probe and `john.ex:329/:353` z3 path report stay; REWORD doctor's ":115 refinement checks will be skipped" message to reference the guard lint instead.
- ALL Class-C "refinement" hits — kernel `refine_branch`/index-goal refinement in `core/kernel.ex`/`certificate.ex`/`elaborator.ex`/`declarations.ex` and Antigen `challenge.ex`/`indexed.ex`/`branch_unify.ex`/`dep_match.ex` (incl. the `:refine` challenge kind) — are DEPENDENT-MATCH refinement, a different concept, untouched. Any diff touching these files' refinement machinery is a defect.
- Historical specs/docs (incl. `2026-07-03-antigen-smt-lint-design.md`) stay as records; `2026-07-08-guard-coverage-lint-design.md` remains live documentation.
- z3 binary discovery (`System.find_executable("z3")`) stays at all four sites: `smt/process.ex`, `doctor.ex`, `john.ex`, and `project/proof/verifier.ex` (its `find_z3/0`, used by the surviving `:smt`-kind `verify_smt/2` — see §1 Peripheral consumers).

## §3 Verification gate

1. Full suite green ONCE at the end; delta vs current baseline = exactly the enumerated deleted tests (plan carries per-file counts); zero surviving-test changes EXCEPT the two named, non-behavioral exceptions: `guard_lint.ex:194`'s comment reword (§2) and `normalizer_test.exs`'s fixture-tag rename (§1 Tests) — `ctor_guard`/`guard_lint` stay byte-identical in behavior and in every executable line.
2. Antigen green with exactly 3 fewer runner rows; `elab/guard_lint` row still green.
3. Oracle replay 65/65 (plan Step 0 verifies NO oracle fixture uses refinement syntax — if one does, STOP for adjudication; none found as of this writing); no `mix cure.oracle`. Additionally: `examples/cure_motif` and `examples/cure_moneta` each run their own `mix test` (their own `mix.exs`, outside `cure.check.examples`) green post-rewrite — this is their actual regression coverage, not the top-level check.examples run.
4. Greps: zero `{:refinement,` (the literal 4-tuple head, comma included) under lib/ and test/ — verified this needs no Class-C exception list: `refine_branch`, `challenge.ex`'s `:refine` challenge kind, and `Indexed.refinement/1` (`def refinement(:well_typed)`/`def refinement(:ill_typed)` in indexed.ex) don't contain the substring `{:refinement,` and none of the eight Class-C files (`core/kernel.ex`, `certificate.ex`, `elaborator.ex`, `declarations.ex`, `challenge.ex`, `indexed.ex`, `branch_unify.ex` generator+assay, `dep_match.ex`) has a single literal hit — a plain grep is already clean there by construction. Also: zero `Cure.SMT.Solver|Translator` references (including prose/comments — e.g. the guard_lint.ex:194 reword above); `Cure.SMT.Process` referenced ONLY by guard_lint.ex and its own tests/doctor/john; `parse_refinement_type` gone; `Std.Refine` appears nowhere under lib/, examples/, docs/, or site/ except `docs/superpowers/specs/*`, `docs/superpowers/audit_categorised.md`, and `CHANGELOG.md` (see §1 Live documentation).
5. `lib/cure/core/` diff EMPTY; `lib/cure/elab/` diff EMPTY except the doctor string is not in elab — so elab diff empty full stop. Ghost commits, explicit pathspec, one mix at a time.
6. STOP conditions: any kernel/elab file needs a change; the `dependent_types.cure` check.examples expectation changes (motif/moneta have no such expectation to change — see §1); any surviving test needs modification beyond the two named exceptions in item 1; an oracle fixture turns out to use refinement syntax; GuardLint behavior (not its one dead-reference comment) changes in any way; either subproject's `mix test` fails post-rewrite.

## §4 Non-goals

- Removing the z3 binary dependency or `Cure.SMT.Process` (GuardLint keeps them).
- Touching GuardLint, the kernel, or dependent-match index refinement.
- Deleting `lib/cure/types/` files beyond the five refinement modules (the rest belongs to #18).
- A "feature removed" parser diagnostic (plain parse error; SMTCoq's return will re-add grammar deliberately).
- Editing historical/audit records: `docs/superpowers/specs/*`, `docs/superpowers/audit_categorised.md`, `CHANGELOG.md`. Everything else under `docs/` and `site/` that makes a present-tense refinement-type claim IS in scope (§1 Live documentation) — this bullet is a narrow historical-record exception, not a blanket "don't touch docs/".

## §5 Acceptance criteria

1. Refinement syntax is a parse error; `Std.Refine`, the SMT query layer (Solver/Translator/Parser), all five classic refinement modules, every `{:refinement,…}` clause, the Antigen smt surface, and the enumerated tests/examples are gone.
2. GuardLint + Process + their test/assay surface byte-identical in behavior (one dead-reference comment reworded per §2, no executable change); doctor message reworded; z3 stays discoverable.
3. §3 greps clean; core/ and elab/ diffs empty; full suite + Antigen + replay green with enumerated deltas; ghost authorship throughout.
