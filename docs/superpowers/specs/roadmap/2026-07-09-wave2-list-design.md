# Value-Surface Wave 2 — `List` builtin family — Design (task #23, Wave 2)

**Date:** 2026-07-09. Second wave of the value-surface parity program (roadmap `2026-07-09-value-surface-roadmap-design.md` D1, hardened fcc768d; program memory `value-surface-parity-program`). Brings `List` and its surface sugar (`[]`, `[h | t]`, `[a, b, c]`) into the DEPENDENT pipeline as a **builtin inductive family with native BEAM-list emit**, following the exact precedent by which `Bool`→atom, `Nat`→integer, and `Sigma`→bare-2-tuple are handled. Runs after Wave 1 `pickup` (landed `f3c0b46`) and after the #22 canonical-spelling kernel batch (landed `7b7f071`+`ea5abbd`).

## 0. Premise (proven by the Wave-2 scout; do not relearn)

- **`List` is a plain 2-ctor `data` inductive — NO Core/TCB change.** `lib/cure/core/term.ex:11-26` enumerates the entire Core grammar (`type/var/pi/lam/app/data/ctor/case/global/int_type/int_lit/float_type/float_lit`); there is no list, cons, or string node, and none is needed. `List` is `{:data, :List, [elemT], []}` with `{:ctor, :Nil, []}` / `{:ctor, :Cons, [h, t]}`. "Nativeness" is an **emit-layer** concern only, exactly as Sigma stays a plain `data` family yet emits as a bare 2-tuple. The kernel (`lib/cure/core/*`) is untouched — the firewall and the #22 gains are preserved.
- **The classic pipeline is a decoy.** `lib/cure/compiler/{codegen,pattern_compiler}.ex` and `lib/cure/types/*` handle lists too, but they are the non-dependent pipeline being deleted. Every change here lands in `lib/cure/elab/*`, `lib/cure/elab/emit.ex`, `lib/cure/core/builtins.ex` (schema + `seed/2` chain + new `list_family`/`list_ctors` helpers — see §1.1/§4, not schema-only), and `lib/std/list.cure`. Read classic only as a behavioral oracle.

## 1. The four moving parts

### 1.1 Seed the `:list` family (so `[…]` sugar resolves in every module)

`Bool`/`Nat`/`Sigma` are each **seeded programmatically** into every module's base env AND **declared** in their `std/*.cure` with a `@builtin(:key)` decorator. List follows the same dual pattern.

- **Schema:** add `list: [{:Nil, 0}, {:Cons, 2}]` to `@schemas` in `lib/cure/core/builtins.ex:14-19`. Ctor **names and arities are load-bearing** — erasure and emit key off `:Nil`/`:Cons` nominally (via the registry, not bare atoms).
- **Seed:** extend `Builtins.seed/2` (`builtins.ex:103-111`) with a `maybe_seed(:list, …)` link in the chain, using `declare_and_register/4` (`builtins.ex:177-182` → `Inductive.declare` → `validate!` → `Inductive.register_builtin(env, :list, fid)`). This makes the `List` family + `Nil`/`Cons` ctors resolvable in **every** module's base env, so the list literal sugar works without `use Std.List` — the same reason Sigma is seeded (its `%[a,b]` and `.1`/`.2` sugar must resolve everywhere).
- **Rationale (decided, lower-risk):** List is seeded, **NOT** added to `@auto_prelude` (`program.ex:234`). `@auto_prelude` imports a module's function *globals* into every module (e.g. `sigma_first`); List needs only its *family + ctors* available everywhere (for the literal sugar), which the seed provides. `Std.List`'s functions (`map`, `filter`, …) continue to require an explicit `use Std.List`. This avoids the auto-prelude qualifier's "must fully dependent-elaborate" coupling (`program.ex:218-233`) and any bootstrap-cycle risk.

### 1.2 Declare `List` in `std/list.cure`

`lib/std/list.cure` currently **references** `List(T)` as a bare magic type name but never declares it (scout item 3). Add, near the top of `mod Std.List`, the canonical declaration with the builtin decorator:

```
@builtin(:list)
type List(a) = Nil | Cons(a, List(a))
```

A parametrized multi-ctor self-referential sum type of exactly this shape is already a named, working precedent in the elaborator, not a new case: `declarations.ex:116` carries the comment `# Parameterized ADT (`type List(a) = Nil | Cons(a, List(a))`)` verbatim, and `elaborate({:container, meta, variants}, env)`'s `:enum` branch (`declarations.ex:103-123`) routes any non-empty `type_params` list through `variant_to_gadt_sig/3` + `declare_parameterized/5` (the same GADT-ctor machinery `Iter`/`IterStep`/`NonEmpty` already exercise for single-ctor parametrized ADTs — `lib/std/iter.cure:58-60`, `lib/std/non_empty.cure:27`).

`Std.List` is a prelude-source key (`prelude_source?/1` at `program.ex:215-216` consults `Cure.Stdlib.Preload.module_groups()`, which walks `lib/std/*.cure` — so any declared `mod Std.X` qualifies). Thus `maybe_register_builtin` (`program.ex:742-753`) schema-validates and registers this declaration when `Std.List` elaborates.

**Reconciliation — verified mechanism (corrects an earlier draft of this section):** the source declaration does NOT reconcile against a pre-existing seeded binding inside `Std.List`'s own elaboration, and `Inductive.register_builtin`'s idempotent-same-fid/hard-error-different-fid behavior (`inductive.ex:178-192`) is *not* what prevents a collision here. The actual mechanism is a **skip**, not a **reconcile**: `Builtins.seed(Env.empty(), declared_type_names(ast))` (`program.ex:134`, `:514`) passes the compiled module's own declared type names as the `exclude` set into `maybe_seed` (`builtins.ex:169-175`), and `declared_type_names/1` (`program.ex:165-168`) will contain `:List` for `Std.List` itself (per the analogous, already-shipped comment at `program.ex:162-164`: "A builtin (Bool/Nat) is NOT seeded into env0 when the module declares its own same-named type — the local declaration is canonical"). So `Std.List`'s own env never gets a seeded `:List` family to begin with; `maybe_register_builtin` is registering the key for the FIRST time in that env, into an empty `builtins` map — there is nothing to reconcile against. (`register_builtin`'s hard-error path exists for a different scenario: two DIFFERENT fids racing to claim the same key within one env — not exercised by Std.List declaring its own family.)

What actually guarantees the seeded copy (used by every *other* module) and the source-declared copy (built when `Std.List` itself elaborates, and merged into any module's env via `merge_env`'s plain `Map.merge` on `families`/`builtins` — `program.ex:689-698`, which does NOT route through `register_builtin` and so enforces nothing on its own) stay in agreement is that both are built to the identical schema (`@schemas[:list]` — Nil/0, Cons/2) and spelled with the same family-name atom `:List`. Bool and Sigma pin this agreement with a **dedicated structural drift test** — `test/antigen/builtin_bool_drift_test.exs` and `test/antigen/builtin_sigma_drift_test.exs` — that elaborates the real `@builtin(...)` source and asserts `Inductive.get_family(prelude_env, fam) == Inductive.get_family(seeded_env, fam)` plus `Enum.sort_by(ctors, & &1.name)` equality, by plain Elixir `==` (no reliance on `register_builtin`). This is the real Bool/Nat/Sigma precedent to follow, not a runtime idempotency check. The executor must write `test/antigen/builtin_list_drift_test.exs` mirroring `builtin_sigma_drift_test.exs` (also folded into §3 as antibody #8); if the seeded and source-declared `:List` families are NOT structurally identical under that comparison, that is a STOP-and-report (do not invent a new reconciliation path, and do not paper over a drift by special-casing `merge_env`).

### 1.3 Desugar `:list` surface nodes → `Nil`/`Cons` ctor form (LOCKED: option i, one early pass)

The parser emits a single tag `:list` for every list form (`parser.ex:759-843`, scout item 4):
- `[]` → `{:list, [line, col], []}`
- `[h | t]` → `{:list, [cons: true, line, col], [head, tail]}` (the `cons: true` meta flag marks a cons cell)
- `[a, b, c]` → `{:list, [line], [a, b, c]}` (literal, N heads, no `cons` flag)
- `[a, b | rest]` → right-nested `{:list, [cons: true], [head, nested]}` chains (`build_multi_head_cons/3`, `parser.ex:837-843`)

**Design fork resolved (scout item 5c; prose-fork guidance → lower-risk):** rather than adding parallel `:list` clauses at every elaboration + pattern + exhaustiveness site, **desugar `:list` nodes to the existing constructor form** (`{:function_call, [name: "Cons"/"Nil", …], args}` — the same shape `Vector`'s `prepend()`/`empty()` use) in **one early normalization pass**, so all downstream ctor/pattern/exhaustiveness/refinement machinery is reused verbatim. The desugaring:
- `{:list, _, []}` → `Nil` nullary ctor call.
- `{:list, [cons: true, …], [h, t]}` → `Cons(desugar(h), desugar(t))`.
- `{:list, _, [e1, …, eN]}` (literal, no `cons`) → right fold: `Cons(e1, Cons(…, Cons(eN, Nil)))`.
- Recursive: sub-elements/sub-patterns are themselves desugared (a list of lists, a cons whose tail is a literal).

This must cover **both expression position and pattern position** (a `:list` node appears identically in a `match` arm's `:pattern` meta — scout item 4). The executor decides the exact insertion point (a dedicated normalization over the surface AST before elaboration, OR a `:list` clause in each of `elaborate_expr_typed`/`elaborate_expr_checked`/`elaborate_expr` + the pattern path that immediately rewrites-and-delegates). The **normalization-pass** shape is preferred because it guarantees uniform coverage of all four parser shapes in every position with no site left behind; if the executor finds a pre-elaboration surface-rewrite hook already exists (e.g. where other sugar like `.1`/`%[..]` is desugared), reuse it. Either way the observable contract is: after this pass, no `:list` node survives to reach an elaborator dispatcher.

### 1.4 Native BEAM-list emit

`lib/cure/elab/emit.ex` special-cases builtin ctors in `lower(env, {:ctor, name, args}, ctx)` (`emit.ex:162-182`) and in branch lowering (`branch_clause/3`, `emit.ex:401-407`), each gated by a registry-keyed predicate (`bool_ctor?` :500, `nat_ctor?` :505, `sigma_ctor?` :513). Add the List analog:
- **Predicate:** `list_ctor?(env, name)` mirroring `sigma_ctor?/2`'s nil-guarded form (`emit.ex:513-516`): `fam = Inductive.builtin(env, :list); fam != nil and Inductive.ctor_family(env, name) == fam`. (`bool_ctor?`'s one-line `==` form, `emit.ex:500`, omits the nil-guard because Bool's presence is unconditional; List is seeded unconditionally too by the time emit runs post-elaboration, so either form is safe in practice — the nil-guarded form is used here because it is the literal precedent named.)
- **Value lowering** (new arm in the `emit.ex:162` cond, placed with the other builtin arms): `Nil` → `{nil, @line}` (the empty-list BEAM literal); `Cons(h, t)` → `{:cons, @line, lower(h), lower(t)}` (a real BEAM cons cell). Directly parallels `sigma_ctor?` → bare tuple.
- **Branch lowering:** add `list_branch_clause` to the `branch_clause/3` dispatch (`emit.ex:401`) matching `{nil, @line}` (Nil arm) and `{:cons, @line, Hpat, Tpat}` (Cons arm), parallel to `sigma_branch_clause` (`emit.ex:413-421`).

Result: `[1, 2, 3]` compiles to the BEAM term `[1, 2, 3]` (real cons cells), and `match xs [] -> … [h|t] -> …` compiles to a BEAM `case` on `[]`/`[H|T]` — interoperable with Erlang/AtomVM list NIFs, not an opaque tagged tuple.

**emit.ex:60 filter is irrelevant** (scout item 8): it drops body-less `builtin_op` defs (K2 arithmetic globals); List ctors are constructor applications inside function bodies, never `builtin_op` entries in `env.defs`.

## 2. Scope (REVISED mid-execution 2026-07-09 — nested patterns are IN scope; the true gap is the bare-top-level-`[]` infer-only body)

> **SCOPE REVISION.** This section originally cut nested list patterns out of the wave on the premise that "the general nested decision-tree lift does not exist" and that `[a,b]`/`[x]`/`last/2` would hit `nested_constructor_arg`. **That premise is false on HEAD** and was discovered by the executor via a test-run (not visible to a code-read of `constructor_pattern/1` alone). The recently-landed Lean-shape general pattern-matching work added a **matrix compiler**, `desugar_nested_arms/2` (`elaborator.ex:2974`), which `elaborate_match/6` invokes (`elaborator.ex:1457`) **before** `constructor_pattern/1` could reject anything. So once the Wave-2 pattern hook `desugar_list_patterns/1` (`elaborator.ex:1446`) rewrites a `:list` arm into `Nil`/`Cons` ctor form, the matrix compiler lowers any *nested* ctor patterns into a decision tree. Nested list patterns therefore elaborate **and run** on HEAD (executor evidence: `[a, b] ->` gives `f([Z,Z])=true`, `f([Z])=false`, `f([])=false`; `[x] ->` and the exact `last/2` `[] / [x] / [_|t]` shape both elaborate). Embracing this is pure parity gain and aligns with roadmap D1's stronger language (below), so the wave now **includes** nested list patterns.

- **In scope (works this wave):** `[]`, `[h | t]`, `[a, b, c]`, and `[a, b | rest]` in **expression** position; and `[]`, `[h | t]`, **and genuinely nested patterns** (`[a, b] ->`, `[x] ->`, `[a, b | rest] ->`) in **pattern** position — the matrix compiler handles the nesting. Native BEAM-list emit must be verified to preserve nested matching (Task 3 adds a nested-pattern *runtime* test, not just an elaboration one — the matrix compiler lowers `[a,b]` to a chain of single-level `[H|T]` matches, each hitting `list_branch_clause`, so native emit should compose, but it must be shown at runtime).
- **The one genuinely ledgered gap — bare top-level `[]` body is infer-only-ambiguous (Finding A).** A standalone `fn e() -> List(Int) = []` returns `{:error, {:unsolved_metavariables, :Nil}}`: this is the **third-dispatch-layer gotcha** (see [[value-surface-parity-program]]). `declarations.ex:elaborate_body` dispatches on the *raw* body node; a `{:function_call, name: "Nil"}` body reaches the goal-bearing constructor branch, but a `{:list, …}` body falls to the infer-only catch-all, and the Wave-2 per-site desugar runs *after* that dispatch, so the goal never reaches it. This is **acceptable and ledgered exactly as Wave-1 pickup ledgered the same layer** — do NOT touch the `elaborate_body` whitelist (its own future increment is the honest fix). The empty-list *value* is fully available in goal-bearing positions: `[]` as a checked argument, `[h | []]`, and a recursion base all elaborate, and a recursion yielding `[]` produces the native empty list at runtime. The directed tests use those goal-bearing positions for the empty-list value, and one ledger-guard test pins the bare-top-level boundary (`{:error, {:unsolved_metavariables, :Nil}}`) so a future `elaborate_body` whitelist wave flips it deliberately.
- **`Std.List` dependent-cleanness.** The executor MUST still audit `lib/std/list.cure`'s actual patterns and report the ratchet honestly (§4). But the old bound ("`last/2`'s `[x]` stays blocked, so `Std.List` cannot flip") no longer holds — `last/2`'s pattern now elaborates via the matrix compiler. `Std.List` may therefore flip **fully** to KEEP this wave, or may stay blocked on a *different* remaining question (the executor flagged `List(T)` type-parameter polymorphism as not-yet-isolated) — report the actual blocker, do not assume. Do NOT rewrite `std/list.cure`'s patterns to dodge anything.
- **Reconciling with roadmap D1.** `2026-07-09-value-surface-roadmap-design.md` D1 (locked) says Wave 2 "must ALSO lift `nested_constructor_arg` for (at least) list-shaped nesting." The original spec scoped that at zero on the (now-false) premise that the lift did not exist. On HEAD the lift **already exists** (the matrix compiler), so D1's stronger deliverable is **satisfied** by embracing nested patterns rather than deferred — this revision brings the spec back into alignment with D1's stronger reading. `non_empty`'s one-deep patterns (`lib/std/non_empty.cure:54-56`) close regardless.

## 3. Antibodies / tests

New test file `test/cure/elab/list_test.exs`, harness mirroring `conditional_test.exs`/`pickup_test.exs` (`Program.elaborate` → `Emit.compile_and_load` → `apply`, compare to the BEAM value). Directed tests (behavioral, not implementation-coupled):

1. **Native representation:** a function returning `[1, 2, 3]` (dependent pipeline) evaluates to the BEAM list `[1, 2, 3]` — assert `apply(...) == [1, 2, 3]`, proving native cons (not a tagged tuple). Include an `is_list/1` assertion to nail nativeness.
2. **Empty list (value proven in a goal-bearing position — see §2 Finding A):** the empty-list *value* evaluates to `[]` (`== []`) via a goal-bearing form (a recursion base such as `drop_all([1,2,3]) == []`, or `[h | []]`) — NOT a bare top-level `fn e() -> List(Int) = []` body, which is infer-only-ambiguous and is instead pinned by the ledger-guard test in antibody 7.
3. **Cons sugar:** `[h | t]` with `h`/`t` params builds the expected BEAM list. Also cover **multi-head cons** `[a, b | rest]` (the right-nested `build_multi_head_cons/3` parser path, `parser.ex:837-843` — a distinct code path from both the plain `[1,2,3]` literal of antibody 1 and the single `[h|t]` cons above; assert the resulting BEAM list is `[a, b | rest_value]`) so `multi_head_cons_test.exs` (§3's oracle-equivalence paragraph) actually has a directed test to cross-check against.
4. **One-deep pattern match:** a `match xs [] -> … [h | t] -> …` function (e.g. a `head`-or-default, or `is_empty`) selects the right arm at runtime for `[]` and a non-empty list.
5. **Element typing:** a list whose elements' types disagree in a checked position is rejected (e.g. `[1, true]` against `List(Int)`), surfacing the ctor/kernel type error — assert error, any shape (not a specific classic E-code).
6. **`Std.List` smoke:** at least one real `Std.List` function that uses only one-deep patterns elaborates + runs through the dependent pipeline (pick one the audit confirms is one-deep, e.g. `cons`/`is_empty`/`head`-like).
7. **Nested list patterns work (positive test — REVISED, see §2):** a `match xs [a, b] -> … [x] -> … other -> …` function elaborates AND selects the right arm at runtime (e.g. `f([Z,Z])=true`, `f([Z])=false`, `f([])=false`) — the matrix compiler (`desugar_nested_arms/2`) lowers the nesting. Include a nested-pattern **runtime** assertion (not just elaboration) so Task 3's native emit is proven to preserve nested matching.
7b. **Ledger guard — bare top-level `[]` body (Finding A):** `fn e() -> List(Int) = []` returns `{:error, {:unsolved_metavariables, :Nil}}` (the third-dispatch-layer / `elaborate_body` infer-only gap, NOT a crash) — assert that exact error shape. This pins the one boundary this wave does NOT close so a future `elaborate_body` whitelist wave flips it deliberately. (This replaces the original "nested-pattern rejected" ledger guard, which is now a positive test per antibody 7 — the real remaining boundary moved from nested-patterns to the bare-`[]` body.)
8. **Builtin drift (§1.2):** `test/antigen/builtin_list_drift_test.exs`, mirroring `test/antigen/builtin_sigma_drift_test.exs` exactly — elaborate the real `@builtin(:list)` source (`Std.List`'s actual declaration, or an inline equivalent), and assert `Inductive.get_family(prelude_env, :List) == Inductive.get_family(seeded_env, :List)` plus `Enum.sort_by(ctors, & &1.name)`-equal ctor lists between `Builtins.seed(Env.empty())` and the prelude-compiled env. This is the real reconciliation check (§1.2) — a runtime idempotency/hard-error path is not exercised by this wave, so this drift test is the only thing standing between the seed and the source declaration silently diverging.

Red-first: each test written and shown failing before the change (list literals fail with `{:error, {:unsupported_expression, {:list, …}}}` pre-change).

**Oracle equivalence (roadmap §3 gate item 6, roadmap §2 Wave-2 bullet):** the roadmap names the classic behavioral pins for this wave's forms — `test/cure/compiler/codegen_test.exs` (list literals), `test/cure/compiler/pattern_compiler_test.exs`, and `test/cure/compiler/multi_head_cons_test.exs` (`[a, b | rest]` right-nesting). The executor reads these as the behavioral oracle for antibodies 1-4 and 6 above (native-cons representation, empty list, cons sugar, one-deep match) — each directed test's expected BEAM value/shape must match what the classic pin already established for the same surface form, not merely "some plausible value."

## 4. Ratchet + firewall gate

- **Firewall:** `test/cure/dependent_pipeline_firewall_test.exs` MUST stay green — no `lib/cure/elab/*` or `lib/cure/core/*` reference to any classic module. (List touches elab/emit + the multi-site `core/builtins.ex` edit (§4 below) + a `.cure` source; none reference classic.)
- **Core untouched:** `git diff` must show `lib/cure/core/` limited to `builtins.ex`, and within it to the sites §1.1 actually names: the `@schemas` map (+1 `list: […]` entry), the `seed/2` pipe chain (+1 `maybe_seed(:list, …)` link), and a new `list_family/0` + `list_ctors/0` private-helper pair mirroring `bool_family/0`+`bool_ctors/0` (`builtins.ex:185-191`) — NOT "a single line" (§1.1's own seed/2 + declare_and_register instructions require the multi-site edit; an executor who edits only the `@schemas` line has not implemented §1.1). The kernel proper — `eval/normalise/conv/quote/kernel/term/erase` — must be EMPTY. State the actual `builtins.ex` site list explicitly at the gate.
- **Ratchet:** re-run the stdlib disposition script (roadmap §0). Wave 2 is expected to flip any module whose only remaining blocker was List toward KEEP; with nested patterns now in scope (§2 revision) `Std.List` itself may flip **fully** (its `last/2` is no longer pattern-blocked). **State before/after counts.** A regression in the existing KEEP set (bool/bounded/decision/equivalent/nat/proof/sigma/vector) = STOP. Report which modules moved and which remain blocked (and on what — if `Std.List` stays blocked, name the actual remaining question, e.g. `List(T)` type-param polymorphism, lambda/String/extern — do NOT assume it's a pattern gap).
- **Oracle replay:** `mix test test/oracle_replay_test.exs` — 100% green, no verdict flipped (List is additive; no existing dependent program should change verdict). Note for the executor: an earlier draft of this spec hardcoded "65/65" here; a file count against `test/oracle/*/*.cure` at review time found 167 `.cure`/`.idr` pairs across 33 cluster directories, not 65 — the corpus has grown since whatever baseline that number came from. Do not hardcode a stale count: read the actual pass count off the live `mix test` run and report `N/N` at the gate.

## 5. Gate

1. Red-first: every directed test shown failing before the change.
2. Scoped `mix test test/cure/elab/list_test.exs test/cure/elab/ test/antigen/builtin_list_drift_test.exs` green (the antibody-#8 drift test lives under `test/antigen/`, outside `test/cure/elab/` — include it explicitly so it's caught by the fast scoped run, not only the full suite); then the full suite ONCE (build lock held; #22/Wave-1 already landed), 0 failures, total = baseline + new tests.
3. Firewall green; `core/` diff limited to the `builtins.ex` sites named in §4; disposition not regressed and reported; oracle replay 100% green (report the live `N/N`, not a hardcoded figure — see §4 note).
4. Commit(s), ghost author, explicit pathspecs. Suggested split: (C1) seed + schema + `std/list.cure` declaration + registration reconciliation; (C2) elaborator `:list` desugaring; (C3) emit native cons + branch. Or a single coherent commit if the executor prefers — each commit must be independently suite-green (per the #22 Part-A precedent). **Ambiguity resolved:** "suite-green" governs the PRE-EXISTING suite at each commit, not `test/cure/elab/list_test.exs`/`builtin_list_drift_test.exs` — those are red-first per §3/gate item 1 and, under the C1/C2/C3 split, will not all go green until C3 lands (C1 alone doesn't support `:list` surface syntax at all yet; C2 supports it but without native-cons emit so antibody 1's `is_list/1` assertion still fails). If splitting into C1/C2/C3, commit the new test file(s) together with C3 (the commit that turns them fully green) rather than earlier — do not commit a partially-red new test file under strict red-green; keep the red-before-each-commit demonstration (gate item 1) as an uncommitted local run, not a committed red state.

## 6. Out of scope

- Closing the **bare top-level `[]` body** (`fn e() -> List(Int) = []`) — the third-dispatch-layer / `elaborate_body` infer-only gap (Finding A, §2). Its honest fix is a future `elaborate_body` whitelist increment shared with pickup and later value forms; pinned by antibody 7b. (NOTE: genuinely nested list **patterns**, deferred by the original draft, are now IN scope per the §2 revision — the matrix compiler handles them.)
- List **comprehensions** — Cure's actual surface syntax is `[x for x <- xs, p(x)]` (the `for` keyword, `parser.ex:1088-1097`; NOT the Haskell-style `[x | x <- xs, p(x)]` an earlier draft of this bullet used — that reads as cons-sugar in this grammar, not a comprehension). `parse_list_or_comprehension` (`parser.ex:759-815`) branches on the `for` keyword into `parse_comprehension`, which produces a `{:comprehension, meta, [body | generators_and_filters]}` node — a **different AST tag from `:list` entirely**, so §1.3's `:list`-only desugar pass never sees it and needs no special-casing to stay out of its way. Not part of the value-surface literal/cons work; separate feature.
- Adding `Std.List` to `@auto_prelude` (decided §1.1: seed only).
- Everything else in the program (lambda-inference, String, @extern, Map/tuples/tail) — later waves.
- Any change to the kernel proper (`core/{eval,normalise,conv,quote,kernel,term,erase}.ex`), to `pickup`/`conditional`/`match`/`bool_case` internals, or to `declarations.ex`'s dispatch whitelist (the third-dispatch-layer gotcha from Wave 1 — a bare top-level list body elaborates infer-only, acceptable here exactly as for pickup; ledger, do not extend without a red test that needs it).
