# Idris-parity roadmap — quality parity for the features Cure already supports

**Status:** planning reference (not an implementation spec). Enumerates every
remaining work item to call Cure's dependent-type implementation "Idris parity"
— **scoped to the quality of the capabilities we already support**, not to
Idris' full feature set. New capability *domains* Cure deliberately does not
have (linear/quantitative types, interfaces at Idris depth, elaborator
reflection, `with`-abstraction as a language feature, named/auto implicits) are
listed once under §4 and then excluded.

This document is the source of truth for "what is left." It is grounded in the
current kernel/elaborator/Antigen code (all line references verified against the
tree at authoring time) and supersedes ad-hoc gap discussions. Individual rows
graduate into their own design specs + autopilot runs; this ledger tracks them.

Related specs:
[`2026-07-02-dependent-match-surface-design.md`](2026-07-02-dependent-match-surface-design.md)
(sub-project ④, in flight — rows 2/8/16),
[`2026-07-02-antigen-eq-rewrite-design.md`](2026-07-02-antigen-eq-rewrite-design.md)
(the `rewrite/eq` vertical),
[`2026-07-01-case-index-unification-design.md`](2026-07-01-case-index-unification-design.md)
(the kernel unifier, row 1),
[`2026-07-01-antigen-tier-a-design.md`](2026-07-01-antigen-tier-a-design.md)
(the Antigen apparatus).

## 1. Scope and framing

"Parity" here means: **for a feature Cure already implements, does it match
Idris in soundness, completeness, and ergonomics?** A gap is either
- *soundness* — the checker can accept an ill-typed / non-total program;
- *reach* — a program Idris accepts (within this feature) that Cure rejects;
- *ergonomics* — Cure demands manual annotation Idris infers;
- *assurance* — how strongly we can *demonstrate* the property holds.

Layers referenced throughout: **K** = trusted kernel / TCB
(`lib/cure/core/*`), **E** = untrusted elaborator (`lib/cure/elab/*`), **P** =
parser/lexer (`lib/cure/compiler/*`), **A** = Antigen
(`lib/antigen/*`, `test/antigen/*`), **C** = eval / codegen / erase.

Status legend: ✅ at parity · 🔵 in flight (sub-project ④) · ⬜ not started · 🔴 live soundness hole.

## 2. Parity ledger — the features we support

| # | Capability | Work item to reach parity | Layer | Kind | Status |
|---|---|---|---|---|---|
| 1 | Dependent case unifier | First-order unification: solution, deletion, injectivity, occurs-check, no-confusion, impossible-branch discharge. **Agda Cycle rule now LANDED** (`f9406b6`, K, first piece of the coverage/totality pivot): a strongly-rigid cyclic index equation `x =?= v` — where the datatype variable `x` occurs under constructor/data spines ONLY in `v` — now discharges the branch as `:impossible` (Agda `Rules/LHS/Unify.hs` `ifOccursStronglyRigid` → `NoUnify`), instead of the earlier conservative degrade to `:undecided`. Two seams in `unify_indices`: `bind_index` (the `ctor ~ var` direction, single-equation `x = MkWr(x)`) and `unify_one`'s final fallback (the `var ~ ctor` direction, incl. the merged `a = k, k = S(a)` ⇒ `a = S(a)` case via resolve-before-bind). Guard is ctor/data-spine-only (`strongly_rigid_occurs?`/`rigid_path_occurs?`): `x = S(x)` is absurd, but `x = f(x)` for a defined `f` is not (`f` may be id) and an `:app`/neutral head halts the search ⇒ conservative-sound. Fixes oracle `cycle/cyc01` (an `impossible`-annotated `MyEq(Nat, n, S(n))` branch, reject→accept); `cyc02` negative control (reachable branch marked `impossible` ⇒ both reject) and `cyc03` (nested `n = S(S(n))`) also same/same. Reverses the two conservative pins it supersedes (`unify_indices_test` (a), `branch_unify_occurs_test`). TCB gate: cycle 3/3, new antibody `cycle_rule_antibody` 3/3 (soundness via an independent closed-Nat reachability oracle — discharge is `:impossible` IFF no witness makes the induced equation convertible), full Antigen 450, oracle replay 61, full suite 2996/0. | K | — | ✅ |
| 2 | Dependent case surface | Impossible clauses (omit + verified `-> impossible`) + constructor-headed motive completeness (verbatim-reuse case) | E, P, C | additive | ✅ |
| 3 | Pattern matching depth | Nested/deep patterns → decision-tree compiler (M8.4). Kernel case already nests, so this is a lowering pass, not a kernel change. **Full pattern-matrix lowering landed** (oracle `match/mt07_nested_patterns` + `match/mt08_multicolumn_patterns`, both accept/accept): `elaborate_match`'s `desugar_nested_arms` groups arms by outer constructor and compiles each group's argument columns with the standard matrix algorithm (Augustsson/Maranget) — `compile_matrix`/`compile_matrix_split` do left-to-right column selection, emitting a tree of single-scrutinee matches (all-variable column → drop + substitute; constructor column → branch per ctor with the sub-columns spliced in, plus a catch-all for the variable rows). Every emitted node is single-level, so motives/index refinement/catch-all/coverage all apply per level and the kernel's own nesting `:case` does the rest. Handles arbitrary depth AND multiple nested columns; deeper nesting lowers on re-entry; verified on the BEAM (multi-column runtime). Shadow-guarded (`:shadowed_nested`), no TCB. **Top-level catch-all mixed with nesting also landed** (oracle `match/mt09_nested_with_catchall`, accept/accept): `desugar_with_default` resolves the catch-all body (binding a named catch-all to a variable scrutinee), weaves it as a wildcard fallback row into every nested group so uncovered sub-patterns fall through, and keeps a `_` outer catch-all for wholly-unmatched constructors — BEAM-verified. Residual edge: a *named* catch-all over a *non-variable* scrutinee (`match S(n) | … | other -> other`) stays a clean `:catchall_with_nesting` (nothing to bind `other` to) — now pinned by oracle `match/mt21_named_catchall_nonvar` (accept/accept, `same`): Cure binds `other` to the elaborated scrutinee and Idris agrees, so the edge is fixtured, not merely asserted | E | additive (refactors match path) | ✅ (depth + multi-column + outer catch-all; named catch-all over non-var scrutinee pinned by mt21) |
| 4 | Pattern forms | Non-constructor patterns in dependent position (`_`, literal, as-patterns) handled, not merely rejected. **Variable/wildcard catch-all landed** (oracle `match/mt06_var_catchall`, accept/accept): a bare-variable (`x -> …`) or wildcard (`_ -> …`) arm covers every constructor not explicitly matched, binding the scrutinee (Idris/Lean variable-pattern coverage). Implemented in `elaborate_default_branch` (E) — each un-matched constructor is reconstructed `cname(fresh…)` (present-arg count from ctor `quantities`), the catch-all name is substituted by that reconstruction, and the branch routes through the ordinary matched-branch path, so index inversion + goal refinement still apply and an unsound catch-all body is kernel-rejected (soundness test: catch-all covering a reachable ctor at a wrong index rejects). Runs on the BEAM (multi-ctor coverage verified). No TCB. **As-patterns landed** (oracle `match/mt11_as_pattern`, accept/accept): `name @ <pattern>` binds the whole matched value AND destructures — parser emits `:at` → arm meta `:as_bind` (disjoint from the declaration-position `@extern` decorator), elaborator `desugar_as_binds` lowers it to `<pattern> -> body[name ↦ <pattern>]` (a pattern and its value-reconstruction share surface shape), which then flows through nested-pattern lowering (composes with `#3` + outer catch-all). **Now nests at any depth** (oracle `match/mt12_nested_as_pattern`, accept/accept): parser emits inline `{:as_pattern}` nodes at a match arm's top level AND inside constructor arguments (`maybe_wrap_as` at `parse_match_arm` + `parse_call_args`/`parse_more_args`); elaborator `strip_as_patterns` recursively removes them, substituting each `name ↦ cleaned-sub-pattern` in the body, and the cleaned nested pattern flows through the matrix compiler. BEAM-verified (nested `t` = whole sub-value); shadow-guarded (`:shadowed_as`). **Tuple/pair patterns** also land (oracle `match/mt13_tuple_pattern`, accept/accept): a Σ/pair is irrefutable, so `try_tuple_match` lowers a single `%[x, y] -> body` arm to the already-supported projections `body[x ↦ p.1, y ↦ p.2]` (Core `{:fst}`/`{:snd}` on the elaborated variable scrutinee — no `{:vdata}`, no new eliminator); scope = variable scrutinee; **n-element and nested tuple patterns** now supported (oracle `match/mt17_nested_tuple_pattern`, accept/accept): an n-tuple type is a right-nested Σ, so `%[e1,…,en]` projects `e1=base.1, e2=base.2.1, …, en=base.2.…2`, and each element may itself be a tuple (recursing on its projection base) — `tuple_subs` walks the spine; BEAM-verified for flat 3-tuples and `%[x, %[y, z]]`. A **lone catch-all arm** (`_ -> …` or `w -> …`) now works on **any** scrutinee type, not just `{:vdata}` (oracle `match/mt18_catchall_nonvdata`, accept/accept): `try_trivial_match` short-circuits a single variable/wildcard arm to `body` / `body[w↦scrut]` after the scrutinee is elaborated (so an ill-typed scrutinee is still rejected) — a Σ/primitive scrutinee no longer needs the vdata dispatch to carry a catch-all. **Records** — declaration + construction — land (oracle `record/rc01_construct_match`, accept/accept): `rec Point\n  x: Nat\n  y: Nat` elaborates as a single-constructor family whose constructor shares the family name and whose argument telescope is *named by the fields* (no separate registry; the kernel treats argument names as labels). Because the constructor and family share a name, `resolve_index_name` (type positions only) is flipped to prefer the *family*, so `p: Point`/`-> Point` are types while `Point(..)`/`Point{..}` are values (a name that is only a constructor, e.g. `Z` as an index argument, is unaffected). Record construction `Point{x: .., y: ..}` desugars to the positional constructor ordered by the field telescope (`desugar_record_construction`), so field order is free and a missing/extra field is rejected. Fields are read back by pattern matching (`match p | Point(a, b) -> a`) or by **field projection** `p.x` (oracle `record/rc02_projection`, accept/accept): a named `attribute_access` looks the field up in the record's constructor telescope and elaborates as a one-branch `match p | Point(f0, …, fn) -> f_i` with the field's own type as the goal (`record_projection`; `.1`/`.2` still mean Σ `fst`/`snd`). BEAM-verified. **Parameterized records** (`rec Box(a)`, `rec Pair(a, b)`) also land (oracle `record/rc03_parameterized`, accept/accept): a parameterized struct is declared through the parameterized-family machinery (`declare_parameterized_struct`) with the constructor's anonymous argument slots renamed back to the field names, and projection instantiates the field's type in the `params ++ fields` frame with the record value's actual arguments (so `val : a` in `Box(Nat)` projects at `Nat`; `snd`'s type resolves through the de-Bruijn-correct parameter substitution). BEAM-verified construction, match, and projection. **Record update** `Point{p | x: ..}` also lands (oracle `record/rc04_update`, accept/accept): it desugars to the positional constructor with each overridden field taking its new value and every other field projected from the base (`base.field`), reusing construction and projection (`desugar_record_update`); an override naming a non-field is rejected. BEAM-verified (overridden field changes, others preserved). *Reach*: a field whose type references an *earlier field* (a genuinely dependent record) **now LANDED** (`90c50db`): both record paths route through the GADT-ctor machinery from a NAMED-domain signature (`struct_ctor_sig`), so the named-binder scope-threading (`7b55d40`) binds each field for the following field types — non-parameterized (`deprec/dr01`) and parameterized (`deprec/dr02`) both reject→accept, mis-typed dependent field (`deprec/dr03`) reject/reject, record cluster rc01–04 unchanged, full suite 2945/0 (retired the old `struct_field_telescope`/`rename_ctor_args`, so `dependent_record_projection` no longer reachable). **Auto-generalization of free type variables** lands (oracle `func/fn07_autogen_id` + `func/fn08_autogen_hof`, accept/accept): a free lowercase type variable in a signature (`fn id(x: a) -> a`) that is not a bound parameter and not a known family is bound as a leading implicit `{a: Type}` (erased), in order of first appearance — so `id`, `const`, and function-type HOFs type without the programmer writing `{a}` (matching Idris). Generalization is restricted to occurrences of kind `Type` (`declarations.ex` `auto_generalize`/`collect_type_vars`): a whole parameter/return type, a function-type domain/codomain, or a family *parameter* slot whose kind is `Type` — an *index* variable (`Vec(a, n)`, `n : Nat`) is deliberately not generalized, so it cannot be mis-bound. The change is additive (only signatures that previously failed on an unbound lowercase name are affected). BEAM-verified. **Forward references and mutual recursion** land (oracle `func/fn09_mutual_recursion` + `func/fn10_forward_reference`, accept/accept): a function may call one defined later in the module. Declaration elaboration is now two-pass — every function *signature* is registered first (`register_signature`), then every *body* is elaborated against the fully-populated environment (`elaborate_function_body`), so a call resolves regardless of definition order (previously `:unknown_global`). Cure accepts *implicit* forward/mutual definitions (no `mutual` block, unlike Idris) — a more permissive but sound choice, since totality is certified separately and best-effort (a non-total group merely stays uncertified, never a soundness hole; the Idris probes are `mutual`-wrapped for a faithful transliteration). BEAM-verified. **Polymorphic functions** via implicit type parameters land (oracle `func/fn05_poly_id` + `func/fn06_poly_const`, accept/accept): `fn id({a}, x: a) -> a = x`, `const`, and polymorphic HOFs (`ap({a}, {b}, f: (a) -> b, x: a) -> b`). A bare implicit `{a}` (Cure's `{name}` erased-argument syntax) carried no kind, so `elaborate_param_telescope` rejected it (`{:untyped_parameter, …}`); a bare implicit type variable ranges over `Type`, so its kind now defaults to `Type` (erased), exactly like `{a: Type}`. The implicit is then solved from the present arguments by the existing metavariable machinery (`elaborate_global_app`/`solve_arg`). BEAM-verified. ~~*Reach*: solving a constructor's implicit parameter from an *expected* type (`fn g() -> List(Nat) = Nil()` — a nullary polymorphic constructor in return position) needs checking-mode constructor elaboration, not yet wired~~ **LANDED** (checking-mode constructor elaboration against the expected type is wired): oracle `retpos/rp01_nil_return` (`fn g() -> List(Nat) = Nil()`, the implicit `a` solved from the expected `List(Nat)`) accept/accept, `rp02_cons_return` (`Cons(Z(), Nil())`, nested — inner `Nil`'s `a` fixed under the `Cons` context) accept/accept, `rp03_wrong_elem_neg` (`Cons(T(), Nil())` where `T : Bool` against `List(Nat)`) reject/reject. **Parameterized (polymorphic) data types** land (oracle `poly/pl01_maybe` + `poly/pl02_list`, accept/accept): `type List(a) = Nil | Cons(a, List(a))`, `Maybe`, `Box`. Two gaps closed. (1) `elaborate_index_telescope` did `Keyword.fetch!(pmeta, :type)`, which *crashed* (`KeyError`) on a bare type parameter `{:param, [], "a"}` — so even a parameterized GADT raised — now a bare parameter's kind defaults to `Type`. (2) the `:enum` elaborator ignored `type_params`; a parameterized enum's positional variants are now synthesized into GADT constructor signatures (each returning the family applied to its own parameters) and elaborated through the shared parameterized-family path (`declare_parameterized`, extracted from the indexed-type clause) with an empty index telescope. BEAM-verified (`Maybe`/`List`/`Box` construct, match, and run). A **nullary polymorphic constructor in return position** (`fn empty() -> Opt(Nat) = None()`, `Nil()` at `-> List(Nat)`) also lands (oracle `poly/pl03_empty_return`, accept/accept): it has no argument to infer its type parameter from, so the constructor body is now elaborated in checking mode against the declared return type (`declarations.ex` `elaborate_body` routes constructor calls to `elaborate_expr_checked`, whose ctor branch pins parameters from the goal via `pin_ctor_result`). BEAM-verified. A **nested underdetermined constructor** in return position (`Cons(Z(), Nil())` at `-> List(Nat)`) now also lands (oracle `poly/pl04_nested_ctor_return`, accept/accept): its inner `Nil()` cannot be inferred, so the inference path fails and an **additive, fallback-only** bidirectional pass takes over (`elaborate_ctor_app_bidirectional`) — it pins the family parameters from the *expected* type (the constructor's result applied to fresh metavariables, unified against the goal), then *checks* each present argument against its field type instantiated with the solved parameters (nesting recurses through checking mode). The fallback fires only when the inference path already errored and only ever *wins when it succeeds* (on its own failure the original inference error is surfaced, so a GADT's genuine `:index_mismatch` is never masked); the assembled constructor is kernel-re-checked, so it accepts only what the kernel independently accepts (a wrong element type is rejected). Restricted to all-present constructors. BEAM-verified (deep nesting; `hd([Z, S(Z)]) = Z`). No TCB. The **inference-mode counterpart** also lands (oracle `poly/pl09_append_nested_args`, accept/accept): a nested underdetermined constructor as a *bare argument* — `app(Cons(Z(), Nil()), …)` where `app`'s implicit `a` has no earlier argument to fix it, so `Cons(Z(), Nil())` must infer standalone — is handled by `elaborate_ctor_app_infer_bidirectional`, which elaborates the constructor's arguments left to right, solving the family parameters from the ones that infer (`Z() : Nat` fixes `a`) and *checking* those that do not (`Nil()` against `Lst(Nat)`), then feeds the pairs to the ordinary `elaborate_ctor_app` (which re-derives parameters and result type, so nothing new is trusted). Additive/fallback-only; BEAM-verified (`append` runs). No TCB. **Constructing an indexed (GADT) value from its constructors** in checking position also lands (oracle `dep/dep01_vector_construction`, accept/accept): `prepend(Z(), prepend(S(Z()), empty()))` at `-> Vector(Nat, S(S(Z)))` — a nested construction whose inner `empty()` is underdetermined until the parameter and index are pinned from the goal. `elaborate_ctor_app_bidirectional` now handles a constructor's *erased index fields*: it seeds a metavariable for the parameters and every field, pins them by unifying the constructor's result against the expected type (solving the parameter `a` and the index `n`), then checks each *present* field against its now-concrete type while threading the pinned erased-index values into the assembled term (the `params ++ fields` de Bruijn frame). BEAM-verified (a length-2 vector; an index disagreeing with the annotation is rejected). No TCB. A **GADT constructor whose result index mentions a free variable only inside a constructor** — `fz : Fin(S(m))` / `FZ : Fin (S n)`, where `m` occurs in no argument — now also elaborates (oracle `dep/dep02_fin_construction`, accept/accept): Idris auto-binds such a variable as an erased implicit index argument, but Cure's `collect_implicit_vars` only picked up index variables that were *bare* arguments of a family application, so a variable nested in `S(m)` was missed and construction failed `:index_mismatch`. `collect_index_expr_vars` now recurses into a constructor application in index position, typing each free variable by the enclosing constructor's field type (so `m` in `S(m)` is inferred at `Nat`). This makes `Fin` — and therefore safe indexing — expressible. BEAM-verified (`fs(fz()) : Fin(S(S(Z)))`; a wrong index annotation is rejected). No TCB. **Dependent-pair construction** also lands (oracle `dep/dep03_dependent_pair`, accept/accept): `%[S(S(Z)), prepend(Z(), prepend(S(Z()), empty()))]` at `-> Sigma(n: Nat, Vector(Nat, n))` — a vector packed with its length. `elaborate_body`'s tuple clause elaborated *both* components by inference (ignoring the return type), so the second's underdetermined parts (`empty()`) stayed unsolved; it now checks the tuple against the return type first (falling back to inference only when that is not a usable Σ), so the second component is checked against the codomain instantiated at the first (`Vector(Nat, S(S(Z)))`). BEAM-verified (length projects to 2). No TCB. **Shared-index dependent matching** (`zipWith`/`lookup` shape) also lands (oracle `dep/dep04_zipwith_shared_index`, accept/accept): matching one vector when a sibling in scope shares its index variable — `match ys` with `xs, ys : Vector(a, S(m))`, and the nested `zipAdd` that recurses on both tails — failed `:branch_type`. `detect_carried_index` fired the carried-equation transport for *any* non-variable (computed) index term mentioned by a sibling, but a **constructor spine** like `S(m)` (or the refined `S(xr_len)` a nested match produces) is *invertible* by ordinary index refinement (Idris's `yr : Vect m a` in the `::` branch) and needs no equation — forcing the transport onto the sibling with a fresh-variable constructor index spuriously failed even when the branch never used it. `invertible_index?` now excludes a constructor-spine-over-variables index from carried-eq detection; only a genuinely non-invertible computed index (a defined-function application like `app(p,q)`) still carries the equation (guarded by `carried_index_sibling_test`). BEAM-verified (`zipAdd([1,0],[1,0]) = [2,0]`, with the inner `match ys` a *partial* match — the `empty` case is impossible by the refined shared index). No TCB. This unblocks the canonical payoff — **length-safe indexing** `lookup : Vector(a, n) -> Fin(n) -> a` (oracle `dep/dep05_safe_lookup`, accept/accept): matching the `Fin` index refines the shared length `n` so each `match v` branch needs only `prepend` (`Fin(Zero)` is uninhabited). It composes the whole dependent stack — GADT `Fin`/`Vector`, a result-only index variable (`fz : Fin(Suc(m))`), and shared-index dependent matching linking the two indices — and is BEAM-verified (`lookup([1,0], 0) = 1`, `lookup([1,0], 1) = 0`). The definition type-checks in either argument order. **Cross-argument implicit solving** now also works (oracle `dep/dep06_cross_arg_implicit`, accept/accept): `lookup(fz(), vec())` — index first — used to fail `{:unsolved_metavariables, :fz}` because the implicit length `n` is determined only by the *later* `vec()` argument, yet `fz() : Fin(n)` was elaborated by inference before `n` was known (and `fz()` cannot infer standalone). The bidirectional application fallback now *defers* an underdetermined argument at a still-metavariable-bearing domain — a placeholder metavariable holds its position so later domains' de Bruijn frames stay aligned — and a second pass (`resolve_deferred_slots`) checks it against the now-solved domain and back-patches the placeholder. BEAM-verified (`lookup(0, [1,0]) = 1`, `lookup(1, [1,0]) = 0`); a genuinely ambiguous call where no argument determines the implicit is still rejected. No TCB. ~~*Reach*: a single self-underdetermined argument with no later solver — `vhead(prepend(S(Z()), empty()))`, where the lone argument must both infer and pin the implicit~~ **LANDED** (`bc08ce0`, E-layer, no TCB): `resolve_deferred_slots` now, before rejecting a still-metavariable deferred domain, calls `solve_deferred_domain` — when the deferred arg is a constructor application it builds the ctor's result-type template over fresh metas and unifies it against the domain, *linking* the domain metavariable to the argument's own index (so `empty : Vector(a,Z)` solves `?n=Z` and `a=Nat`), then walks the ctor's present fields (recursing on a nested underdetermined ctor). Meta-solving only; the term is still built by checking-mode elaboration and kernel-re-checked by `finish_global_app`. Oracle `erasedidx/ei02_vhead_typeparam` reject→accept, `ei01` unchanged, `ei03_ambiguous_neg` (`vhead(empty())`, `Z` vs `S(n)`) reject/reject preserved; dep/poly/frp/dotpat unflipped; full suite 2947/0. ~~*Reach*: dependent-pair construction with an *implicit* element type **and** a nested construction (`%[.., prepend(x, empty())]` under `{a: Type}`) still leaves the innermost `empty()` unsolved~~ **LANDED** — closed by the same `bc08ce0` deferred-domain solving (the ctor-result-type unification links `empty`'s index even under a Σ binder + an implicit `{a}` in scope). Oracle `dpair/dpp01_poly_nested` accept/accept, `dpp02_wrong_len_neg` (pair witness `Suc(Suc(Zero))` vs a length-1 vector) reject/reject. *Reach*: the same construction in *inference* position — as the argument of an implicit-parameter call (`vhead(prepend(S(Z()), empty()))`) — ~~needs inference-mode solving of the erased index from a present field's type, not yet wired~~ **LANDED** (`bc08ce0`; see `erasedidx/ei02` and the deferred-domain solving in `resolve_deferred_slots`); and a nested dependent `match` linking two indices (`lookup(i: Fin(n), v: Vector(a, n))`) **works in BOTH directions** (oracle `dep/dep05_safe_lookup` matches `i` first — `fz` refines `n=Suc(m)` so the inner `v` match discharges `empty`; `dep/dep12_vfirst_lookup` matches `v` first — `empty` refines `n=Zero` so the inner `i` match discharges the uninhabited `Fin(Zero)` by an empty match — both accept/accept). An **implicit parameter solved from an earlier argument, unblocking a later underdetermined argument** now also lands (oracle `poly/pl05_implicit_solved_arg`, accept/accept): `firstOr(Z(), Cons(S(Z()), Nil()))` where `firstOr : {a} -> a -> Lst(a) -> a` — the list argument is underdetermined in isolation (its inner `Nil()`), so up-front inference fails, and an **additive, fallback-only** bidirectional application (`elaborate_implicit_app_bidirectional`) retries: it folds the callee's Π telescope left to right, a fresh metavariable per erased slot, and for each present slot instantiates+zonks the domain — *checking* the argument when the domain is metavariable-free (so `Cons(S(Z()), Nil())` reaches checking mode against `Lst(Nat)` once `a = Nat` is fixed from the first argument) or *inferring and unifying* when it is not. Reached only after inference already failed and only *wins when it succeeds* (original error preserved otherwise); `finish_global_app` assembles and the kernel re-checks, so a wrong-typed argument is rejected. BEAM-verified (`firstOr(Z, [S(Z)]) = S(Z)`). No TCB. **Polymorphic `map` now type-checks and runs** (oracle `poly/pl06_polymorphic_map`, accept/accept): re-passing a function-typed argument (`map(f, xs)` with `f : (a) -> b`) while solving the callee's implicit parameters previously hit an off-by-one de Bruijn error (`{:var, 4}` vs `{:var, 5}`) — unifying `(a) -> b` against `(?a) -> ?b` crosses the Π binder, and a metavariable solved *inside* the codomain was recorded one level too deep, reappearing mis-levelled in the result type. `Cure.Elab.Unify` now tracks binder depth and **strengthens** a metavariable's solution back to the ambient frame before recording it (a free variable that would escape the removed binders fails the solve; at depth 0 — every prior unification — strengthening is the identity, so existing behaviour is unchanged). BEAM-verified (`map (+1) [0, 1] = [1, 2]`). No TCB. The **single-implicit endomorphism argument `(a) -> a`** (same variable in domain and codomain) now also works (oracle `poly/pl07_endo_map`, accept/accept): it needed the *dual* of the strengthen fix — a solved metavariable read *under* the codomain binder is shifted *up* into the current scope (`force_d` in `Unify`), so `?a` solved from the domain matches `a` under the codomain binder. The two directions (strengthen-down on solve, shift-up on force) keep both `(a) -> b` and `(a) -> a` correctly levelled. BEAM-verified (`emap (+1) [0, 1] = [1, 2]`). No TCB. A **polymorphic call used as an argument to another call** (`hd(Z(), map(s, mklist()))`) now also works (oracle `poly/pl08_map_as_argument`, accept/accept): the scoped application path the non-implicit outer call used bound arguments positionally and did not insert metavariables for a *nested* implicit call, so its explicit argument was mis-bound to the `{a} : Type` slot (`conversion_failure` against `Type`). When that scoped path fails, `elaborate_named_call` now retries via `elaborate_bidirectional_app`, which checks each argument against the callee's Π domain — so the nested implicit call elaborates in checking mode (where it already works). Additive (reached only on failure, original error surfaced otherwise); BEAM-verified (`hd(map (+1) [0, 1]) = 1`). No TCB. ~~*Reach*: an argument underdetermined by *all* arguments (`mk(Cons(Z(), Nil()))`) still needs the call's *expected return type* to flow in.~~ **LANDED** (`3c0d9ee`, oracle `retflow/rf01_phantom_from_return` accept/accept, `retflow/rf02_ambiguous_neg` reject/reject): the caller's *expected return type* is now threaded through `elaborate_global_app`/`elaborate_implicit_app_bidirectional`/`finish_global_app` and unified against `Subst.instantiate(codomain, chosen)` in the metavar context BEFORE the has-meta rejection — so an implicit determined by neither the arguments nor a later sibling, but fixed by the call site's expected type, is solved (`mk(Cons(Z(), Nil())) : Wrap(Nat)`). Checked-mode function-call dispatch retries via `elaborate_global_app_expected` and `elaborate_function_body` additively re-routes a bare-call body through the checked path. Meta-solving only (`finish_global_app` kernel-re-checks), sound by construction; a *genuinely* ambiguous call with no expected type still rejects (`rf02_ambiguous_neg`, faithful to Idris). **Type aliases** also land (oracle `alias/al01_type_alias`, accept/accept): `type MyNat = Nat` was silently dropped — `Program.declarations/1` kept only `:container`/`:indexed_type`, so the `:type_annotation` never reached the declaration elaborator and `MyNat` resolved to an opaque `{:global, MyNat}` that would not convert to `Nat`. The alias is now registered as a nullary def `MyNat : Type := Nat` (`declarations.ex`), which conversion δ-unfolds, so an alias is interchangeable with its RHS in every position the kernel resolves by *conversion*: parameter types, return types, and value/constructor arguments. BEAM-verified. **Function-type aliases** now also land, including *use* (oracle `alias/al02_function_type`, accept/accept): `type Endo = (Nat) -> Nat`, then a parameter `f: Endo` applied `f(x)`. Two parts, both outside the TCB. The parser accepts a function-type alias RHS (a leading `(` routes to the type-expression parser instead of the variant parser — previously an `:unexpected_token`). And `build_context` (E) weak-head-normalises each parameter type before handing it to the kernel, so an `Endo`-typed binder is stored as the underlying Π the kernel's `ensure_pi` inspects when the parameter is applied (previously the alias stayed a neutral global → `:not_a_function`); the whnf is conversion-preserving (the alias is definitionally its RHS), so no new terms become well-typed and the kernel is untouched. BEAM-verified (`ap(inc, Z()) = S(Z)`). *Reach (TCB-gated)*: `match` on an alias-typed scrutinee inspects the scrutinee type's *head* in the trusted kernel (`:case` rule) without weak-head-normalising first, so it still rejects an alias; unfolding there is a kernel change held for review. **Single-constructor data types** also land (oracle `match/mt20_single_ctor_type`, accept/accept): `type Box = MkBox(Nat)` used to parse as a `:type_annotation` (alias) because only a `|`-separated body became an `:enum` container, so `Box` was never a family and a `match` on it failed `:match_scrutinee_not_data`. The parser (`variant_ctor?`) now treats a lone constructor *with fields* as a one-constructor family; a genuine alias RHS (a plain type expression) stays a `:type_annotation`. BEAM-verified. A **tuple pattern inside a constructor argument** (`A(%[x, y])`) also lands (oracle `match/mt19_tuple_in_ctor`, accept/accept): `desugar_tuple_args` rewrites each tuple constructor-argument to a fresh `$tup_i` binder with `x ↦ $tup_i.1`, `y ↦ $tup_i.2` in the body, so `A(%[x,y]) -> body` becomes the ordinary all-variable `A($tup_0) -> body[…]` and composes with Σ-typed fields; BEAM-verified. **Pair value construction** in inference position also lands (oracle `match/mt14_pair_construction`, accept/accept): `%[a, b]` as e.g. a bare function argument was `:unsupported_expression`; `elaborate_expr/3` now lowers it to Core `{:pair, …}` and `Kernel.infer` types the enclosing application by checking the pair against the callee's Σ domain (so a dependent Σ parameter is honoured too — no synthesized type is trusted). BEAM-verified construct-then-match. Σ-typed **constructor fields** also land (oracle `match/mt15_sigma_field`, accept/accept): `type_to_core` gained a non-dependent `{:sigma_type}` clause (constructor field telescopes are non-dependent, so the Σ codomain has no free binder), kernel-validated by `check_family` — pairs now live inside data and compose with `%[…]` construction + `.N` projection for a full build-a-record / read-a-field round trip. **Let-bound pairs project** (oracle `match/mt16_let_pair_projection`, accept/accept): `let` is substitution-based, so `let p = %[a,b]` then `p.2` inlines to `%[a,b].2`; a new `elaborate_expr_typed({:attribute_access, …, [{:tuple, …}]})` clause applies the Σ β-rule (`fst %[a,b]=a`, `snd=b`) directly — no pair term built, no bare-pair inference. BEAM-verified. **Literal patterns, `if`, and `when` guards on a primitive scrutinee now all land** on the committed **dependent Bool eliminator** `bool_elim` (`Bool.rec`, the minimal primitive eliminator — a *reviewed, gated* TCB addition in `lib/cure/core/*`; the surface features below are untrusted E-layer desugarings on top, re-checked by the kernel). (a) **Literal patterns** (oracle `cond/cond02_int_literal`, accept/accept): `match n | 0 -> a | 1 -> b | m -> c` on Int/Bool/Float desugars to a right-nested `bool_elim` chain testing `{:prim, :eq}` (an exhaustive Bool match compiles directly to `bool_elim`); `try_literal_match`/`literal_chain`, BEAM-verified. (b) **`if/then/else`** (oracle `cond/cond01_if`, accept/accept): `elaborate_expr_typed`'s `{:conditional}` clause builds `bool_elim c (λ_.T) t e` with the constant motive, both branches checked at the goal; BEAM-verified. (c) **`when` guards** (oracle `guard/guard01_simple` + `guard/guard02_chain`, accept/accept): `match n | x when g -> a | x -> b` desugars to `bool_elim g[x↦n] a[x↦n] b`, each guarded arm testing its guard and *falling through* (the `ff` branch) to the remaining arms, the chain closed by an unguarded catch-all (a still-guarded final arm is non-exhaustive → rejected); `try_guard_match`/`guard_chain` claims every guarded match (handles the variable-scrutinee subset, errors on the rest, so no shape-dispatch path silently drops a guard), shadow-guarded, BEAM-verified. Guards also required **surface binary operators** to elaborate: `{:binary_op}` now lowers to the kernel primitive `{:prim, op, …}` (`x == 0` → `{:prim, :eq, …}`), the kernel's `infer_prim` fixing the result type; arithmetic/comparison/equality/connectives, BEAM-verified. **Guards on constructor patterns now land** (oracle `guard/guard03_ctor_pattern`, accept/accept; adversarial `guard/guard04_ctor_pattern_neg`, reject/reject): `match n | S(k) when g -> a | S(k) -> b | Z() -> c` — a guard on a constructor pattern (previously rejected, since `try_guard_match` only handled variable/catch-all patterns). Idris has no pattern guards; it collapses such an arm into an `if` inside that constructor's `case` branch, falling through to the next same-constructor arm when the guard is false. An additive E-layer pre-pass `desugar_ctor_guards` reproduces exactly that: it folds each constructor group carrying a guard into ONE guardless arm `C(w…) -> if g₁ then a else … else <closer>` (closer = the group's trailing unguarded arm, or the match's catch-all bound to the scrutinee), so the result flows through the ordinary `:vdata` match path and the `if`s lower to the committed `bool_elim` — no matrix-compiler or kernel change. `reject_nested_guards` bails up front on a guard over a *nested* constructor pattern (the matrix compiler runs first and does not thread guards) — deferred, not silently dropped; only groups containing a guard are folded (a plain group passes through so a genuine duplicate constructor still reaches the downstream check); a still-guarded last row with no catch-all is non-exhaustive → rejected. The adversarial control (wrong-typed guarded body) rejects because the folded body is kernel-re-checked, so no blind-accept. **Guards on *nested* constructor patterns now also land** (oracle `guard/guard05_nested_ctor_pattern`, accept/accept; adversarial `guard/guard06_nested_ctor_pattern_neg`, reject/reject): `match n | S(S(k)) when g -> a | S(S(k)) -> b | S(Z()) -> … | Z() -> …` — a guard on a nested pattern, threaded through the pattern-matrix compiler. Design grounded in the Idris/Lean/Agda reference clones: **none exposes boolean guard fall-through as a primitive** — Agda and Idris-*surface* eliminate it via `with`-abstraction (the condition becomes an extra *total* scrutinee of a generated with-function; `Agda/TypeChecking/With.hs`, `Idris2/TTImp/WithClause.idr`), Lean via a *filtered* single decision tree with total leaves (`Lean/Meta/Match/Match.lean`); only Idris' `CaseBuilder` threads an explicit failure continuation (`match … (err : IMaybe (CaseTree vars))`, the `errorCase` weakened under each constructor's binders — `Core/Case/CaseBuilder.idr`). That is the Wadler/Augustsson `match … default` algorithm, and it is what Cure adopts, with one simplification: **Cure's matrix works on surface *names*, not de Bruijn**, so the fall-through is just the row-list tail (already materialised by `desugar_with_default`'s catch-all weaving) — no weakening/shifting bookkeeping. Matrix rows carry an optional guard (`{patterns, guard, body}`, substituted identically to the body at every column split); each leaf's reached rows fold into a `bool_elim` `if`-chain via `fold_leaf_rows` (a guarded row falls through to the next reached row, an unguarded row terminates — so guardless matrices are byte-for-byte unchanged); a guarded final row with no unguarded successor is non-exhaustive → rejected. No matrix-external default parameter, no TCB. *Reach*: a non-variable guarded scrutinee (needs a let-binding to avoid duplication); join-point *sharing* of a large duplicated default (Lean's filtering / GHC join points) is a follow-up blocked on the same let-block reach as #26; and BEAM-runtime verification of a constructor-guard program is blocked by an orthogonal, pre-existing front-end limitation (the standalone `cure-avm` checker cannot pass a user-ADT constructor value as a function argument — `id(S(Z()))` fails identically), so this is verified at the parity layer (dependent elaborator + kernel), not on-device. **Investigation 2026-07-04 (both residuals share ONE root cause — the `let` binder):** the guard compiler rejects a non-variable scrutinee at `bind_catchall_body` (elaborator.ex:2352) and `guard_bind` (2367) with `{:unsupported_guard, :complex_scrutinee}`, and join-point sharing rejects at `resolve_default_body` (2710) with `:nonvariable_scrutinee` — because both paths refine/share by SURFACE SUBSTITUTION (`subst_surface_var`) of the pattern var with the scrutinee EXPRESSION, which would duplicate (and re-evaluate) a complex scrutinee across the `bool_elim` chain / shared default. The identical mechanism underlies `elaborate_let_block` (3330: `rest |> Enum.map(&subst_surface_var(&1, name, rhs))`), which is why the #26 caveat ("`let` inlines `e` at each use, does not bind-once") is the SAME gap. **Fix design (one E-layer primitive, no TCB):** a bind-once binder that elaborates `e` once to `e_core : T`, checks the continuation/body against `shift(expected,1,0)` in the `x:T`-extended context, and emits the Core β-redex `{:app, {:lam, T, body_core}, e_core}` — the kernel β-reduces it, evaluating `e_core` once. Typing is always clean because the outer `expected` never mentions the fresh `x` (introduced by the binder), so `shift`-then-β-substitute returns `expected` exactly; the dependent-`let` case (body TYPE depends on `x`'s value) is the only part that must stay on substitution or be a follow-up. This single primitive unblocks (a) non-variable guarded scrutinee (wrap the guard chain in the β-redex, run `guard_chain` with the fresh binder VARIABLE as scrutinee), (b) join-point sharing of a shared default, and (c) closes the #26 `let`-duplication caveat. Probe-first, red-green via a `match <complex-expr> when …` oracle cluster + a soundness-negative. **Non-variable guarded scrutinee now LANDED** (`6122340`, E-layer, no TCB): `try_guard_match` now routes a non-variable scrutinee to `bind_once_guard`, which elaborates it once and emits the β-redex `(λ s:T. <guard chain over s>) e` — the guard chain substitutes only the VARIABLE `s` (no duplication/recompute), and since the outer goal never mentions `s` it is shifted under the binder and the redex checks back against `expected` exactly. Oracle `guardscrut/gs01_nonvar_guarded` reject→accept, `gs02` neg reject/reject (wrong-typed guarded body still rejected by both); existing `guard` cluster unchanged; full suite 2939/0. A named catch-all over a non-variable scrutinee already worked at single level (`gs03` accept/accept, via `elaborate_default_branch`). **Nested named-default over a non-variable scrutinee now LANDED** (oracle `match/mt22_nested_named_default_nonvar`, accept/accept): a *named* default (`other -> …`) meeting a *nested* arm over a *non-variable* scrutinee (`match S(n) | S(Z()) -> … | other -> …`) previously rejected `:catchall_with_nesting` (`resolve_default_body` had nothing to bind `other` to). `elaborate_match` now detects this shape (`hoist_named_default?`) and hoists the scrutinee into a fresh `let $s = scrut in match $s | …` (`hoist_named_default_scrutinee`), so the whole variable-scrutinee path applies and `other` binds `$s` — evaluated exactly once through the surface `let`, matching Idris' `case … of other =>`. BEAM-verified (`f(Z)=S(Z)`, `f(S(Z))=Z`); the former `nested_pattern_test` boundary case now asserts acceptance + correct binding. E-layer, no TCB, full suite 4239/0. **Residual (niche, efficiency-only):** join-point *sharing* proper — the Lean/GHC optimization of not duplicating a large shared default body across the woven groups — is a codegen perf concern, not a reach gap. The general `let`-bind-once (which would also close the #26 `let`-duplication caveat) remains a separate follow-up. Z3 guard-coverage lint landed (spec 2026-07-08-guard-coverage-lint): provably-exhaustive guard chains (trichotomy-style, Int fragment) no longer need a catch-all at the `guard_chain` site — a deliberate extension beyond Idris, foundation-spec-authorized; ctor-group sites stay conservative (K13), shadowed guards warn | E, P | additive (bool_elim: reviewed TCB, gated) | ✅ (catch-all + as-patterns incl. nested + tuple + literal patterns + `if` + variable-pattern guards + constructor-pattern guards incl. **nested** + **non-variable guarded scrutinee** + **nested named-default over non-var scrutinee** + **shared default over non-var scrutinee** (`match/mt23_shared_default_nonvar`, accept/accept), all via `bool_elim`; no reach/soundness gap remains — the `:nonvariable_scrutinee` rejection at `resolve_default_body` is now provably intercepted by the `hoist_named_default?` bind-once hoist for every named-default-over-non-var shape. The only residual is join-point SHARING: when an accepted shared default fans out to several reconstructed missing-constructor branches, its body is duplicated rather than emitted as one join point — efficiency-only (Cure is total, so duplication changes code size, not the accepted-program set or the runtime value), outside the parity axes) |
| 5 | Pattern matching | **✅ CLOSED (2026-07-08) — named-implicit tail landed (`dotsyntax-tail`).** Forced/dot patterns + forced-argument erasure. **What landed (K+P):** the kernel index unifier `unify_indices`/`bind_index` now performs the **forced-equation Solution step** (Agda `unifyIndices`): when matching a constructor induces an equation between the scrutinee's index variables (e.g. `same : SameLen(k,k)` matched against `SameLen(a,b)` forces `b:=a`), it resolves-before-binds into a forced substitution instead of dropping the equation as `:undecided`. This is the actual Idris-parity REACH — `dp01b` (matching forces `b:=a`) flips reject→accept, matching Idris. A union-find `resolve_index_var` guard prevents multi-key cyclic substitutions (`T(a,a,b,b)` vs `T(i,j,j,i)` → correct `i≡j` collapse, not a swap); found and fixed via the TCB gate. The elaborator already applies the substitution unfiltered, so no elab change was needed for the reach flip. Parser accepts leading-`.` `{:forced_pattern,…}` syntax with an expression-position guard (groundwork). **Forced-arg erasure: vacuously satisfied** — in Cure, forced index positions are *structurally always erased implicits* (constructor sigs have no named binders; only family-inferred index vars flow into result indices and they are `:erased`; no `:present` arg is ever index-forced), so `emit.ex` already anonymizes every forced position — nothing to erase. **Explicit dot-pattern elaboration — LANDED (2026-07-04, `5409184`) via named-implicit dot patterns.** A forced erased-implicit index can now be hand-annotated in a constructor pattern: `vcons({k = .m}, h, r)` asserts the forced index `k = m`, convertibility-checked against the value branch-unification pinned (`Conv.conv?`); a wrong dot (`{k = .(Z())}`) is rejected `{:forced_pattern_mismatch,…}`. P+E only (parser `{IDENT = expr}` prefix + `constructor_pattern`/`elaborate_matched_branch`; unchanged return shape → no consumer cascade; no kernel/erasure change — the position was already erased). Oracle cluster `nidot` (ni01 accept, ni02 reject). *Tail closed (2026-07-08, `dotsyntax-tail`, E-layer only — no kernel change):* **C-a** — the forced check now runs on the **carried-eq branch path** too (`elaborate_carried_eq_branch` receives the surface pattern + verdict subst and runs `check_named_implicits` in the pre-proof branch frame), so a wrong dot on a carried branch rejects instead of being silently discarded. **C-b** — pattern value-reconstructions are stripped of `{:named_implicit_pat,…}` annotation nodes at **both** `refine_scrutinee_in_body` and `desugar_as_patterns`/`strip_as_patterns` (via `strip_named_implicits`), so a branch body referencing the scrutinee (or an as-pattern over a named-implicit ctor pattern) elaborates instead of spuriously erroring `{:named_implicit_not_in_pattern,…}`; the annotation is still checked on its own path against the un-stripped pattern. **C-c** — an **unforced** named implicit written as a **bare variable** now **binds** the anonymized erased telescope slot at quantity 0 (`split_named_implicits` names the slot in `branch_names`), with `Cure.Elab.Relevance` extended to fold the matched constructor's own erased-field quantities into the tracked set so a *relevant* use of the bound variable is rejected `{:erased_used_relevantly,…}` (Idris-faithful per the 2026-07-08 idris2 probes: `MkP {k = kk} v` binds and accepts under erased-only use, rejects under relevant use); an unforced **dot/non-variable** inner still rejects `{:named_implicit_unforced,…}` (documented hint: write a bare variable to bind). Oracle `nidot` ni01–ni08 all `same` (ni03/ni07's carried-shape simplified to a two-index invertible-forced family both languages express — Idris cannot match a genuinely stuck carried index without `with`; the carried-path forced check itself is covered end-to-end by the `dotsyntax-tail` unit tests + the Antigen dot-forcing vertical's H/hmk carried-shaped seeds). **dp01/dp02 UPDATE (2026-07-04): now ACCEPT.** The independent auto-generalization defect (Type-param family + index var repeated across ≥2 positions → `check_result_indices` seeded `do_spine` with an empty env → de Bruijn mis-levelling, spec 2026-07-04-forced-patterns §1.2) was FIXED (`dc2b635`, kernel: seed the parameter neutrals as `check_ctor_app` already did) and merged into this branch (`2689d7a`, full suite 2926/0). Oracle `dotpat` dp01/dp01b/dp02/dp03 all `accept/accept` `same`; dp01 stays the *implicit* forced-match control. **Remaining tail — RESOLVED (2026-07-04, oracle-proof investigation complete, no new syntax warranted).** Empirically proved (probes: a `(k: Nat) -> …` present-binder ctor sig is a *parse error* — `parse_type_atom` has no named-binder form; and a plain-var match on a would-be present index arg rejects while Idris accepts) that a **bare standalone positional `.e`** is *structurally inexpressible* for a forced index in Cure: constructor signatures have no named present binders, so every forced index is an auto-generalized *erased implicit* reachable only *by name*. The expressible explicit-dot form therefore **is** the landed named-implicit `{name = .e}` (`5409184`); building bare-positional `.e` elaboration would be dead syntax (`constructor_pattern` already rejects a non-variable positional, correctly, since no present arg is ever index-forced). Parser (bare `.e` → `{:forced_pattern,…}`) + expression-position guard already landed (`852742a`, `dot_pattern_parse_test.exs` a–d). The material negative-checking value is now realized on the *polymorphic* `MyEq` family: **dp02 rewritten to the explicit dot `mrefl({w = .a})`** (accept/accept, TODO resolved) and **new `dp06_dot_mismatch_neg`** — wrong dot `mrefl({w = .(S(a))})` — reject/reject `same` (both Cure and Idris reject; `S(a)` not convertible to the forced `a`). Oracle `dotpat` now dp01/dp01b/dp02/dp03 `accept/accept` + dp06 `reject/reject`, all `same`; P+E only, no kernel/erasure change. **Update (2026-07-04):** the local type/constructor shadowing correctness defect (`{:missing_branch,_}` when a local `type Nat = Zero|Suc` failed to disown imported ctors) that gated this path is now FIXED — E-layer re-keying + uniform bare-name shadowed resolution (per-name scoping as in Idris/Agda), oracle `shadow` cluster 01–09 all `same`; spec/plan `2026-07-04-local-type-shadowing-{design,plan}.md`. Verified: TCB gate green (antibody 6/6 + full Antigen + full suite), oracle `dotpat` cluster. Spec `2026-07-04-forced-patterns-design.md`, plan `…-forced-patterns-plan.md`; tail `2026-07-08-dotsyntax-tail-{design,plan}.md` | K, P, E | additive | ✅ |
| 6 | Dependent matching | `with`-abstraction (match on an intermediate, refine the goal) — *borderline; core to Idris matching quality*. **Capabilities A + B + sibling refinement landed** (`58037d6`, `8487c51`, `dbf874e`): block/inline `with <single-expr> [proof <name>]` over a **non-indexed** scrutinee. **A** refines the GOAL by the scrutinee's *value* via a value-abstracting motive `{:lam, ty, abstract_term(goal, e, 0)}` (the `motive_for` pattern, not index-refinement), in place as `{:case,…}`; strictly beyond `match` (oracle wi01: plain `match` on the same goal is rejected). **B** (`proof`) binds `<name> : Eq(T,e,pat)` per branch via an **Eq-arrow motive** `λw. Eq(T,e,w) → G[e↦w]`, branch `λ(pf).body`, `:case` discharged by `refl(e)` (oracle wi04, load-bearing). **Sibling refinement** (oracle wi05/wi06): in-scope params whose type mentions `e` are refined per branch by **proof-carrying transport** `{:rewrite, prf, λx.H_j[e↦x], h_j} : H_j[e↦pat]` (reuses the `:rewrite` primitive, whose checking is `Eval.apply`, not `reify`) and re-bound under the original name — composes with `proof`. Independent-sibling set only (`{:with_sibling_dependency_unsupported,_}` otherwise). Kernel accepted all three unchanged (no TCB). **Runs on BEAM** (`d5fc309`): value-level `with` lowers to a runtime `case` (refinement + proof erase; `proof` binds an erased placeholder), verified end-to-end (compiles, loads, runs identical to `match`). **LHS re-matching over an indexed view landed** (`bb17c88` P, `4f686fc`+`e96fff8` E): a with-clause may restate the parent LHS refined — `<parent-pat…> \| <with-pat> -> body` (parser `{:with_rematch_arm}`) — and `elaborate_with_rematch` routes it like an indexed `match`: `build_motive` generalizes the goal over the scrutinee's index vars, and the kernel's index inversion (`branch_unify` `{:solved, n:=S m}`) refines the branch goal AND every index-mentioning sibling (`w:SNat(n)` ↦ `SNat(S m)`) via `specialize_branch_context_subst`. `match_parent_lhs` (ports Idris `getMatch`) validates each restated LHS is constructor-refined (rejecting forced/`k+k` patterns). This is the **convoy**, and it is **sound with NO TCB change**: the index equation comes from the case eliminator (not index injectivity); the kernel validates every `{:data}` slot-split against the signature (`check_spine`), the split is not part of value identity (flat `{:vdata}`), and `conv?` is value-directed so the `reify` collapse equates no distinct normal forms (all three positively established + banked as Antigen antibodies `data_split_validation`/`reify_collapse_distinct`). Oracle wi07 (faithful indexed Nat-view) is now accept/accept. *Reach*: multiple with-exprs, named/dependent ctor args (P-layer — the singleton `SNat`-carrying workaround is used meanwhile), forced/arithmetic restated patterns (#5), nested/multiple `with`, guards in rematch arms, runtime use of a proof *term*. The residual `Quote.reify` split-collapse false-rejection on the **Eq-endpoint** path of `infer_type_value_sort` is now **CLOSED** (B3): signature-aware `reify` (TCB, Agda/Lean prior art) recovers the param/index split, so the reach pin `reach_reify_split.sexp` was migrated to `corpus.sexp` — see TCB note (2) below. **Nested `with` landed** (`d411080`, oracle `withmulti/wm02_nested_with` accept/accept, `wm03_nested_neg` reject/reject): a with-clause appearing as another with/match arm body was unhandled — `:with_abs` was dispatched only at the function-body top level (`declarations.ex`) and in codegen, never in checking-mode expression position, so a nested `with` rejected at elaboration while Idris accepts. Fixed by mirroring the `:pattern_match` clauses for `:with_abs` in `elaborate_expr_checked` and `elaborate_branch_body` (both delegate to the proven `elaborate_with/8` with the branch's already-refined goal as the checking-mode expected type), so each nested level refines on top of the enclosing branch's goal and with-abstractions compose. Soundness pinned by `wm03` (an inner branch body wrong at the refined goal is rejected by both Cure and Idris). Additive, no TCB; full suite 2928/0. *Reach*: multiple-`with` SURFACE sugar (`with e1 e2`, arms `p1, p2 -> …`) is the separable follow-up — a parser desugar onto this nested form. **Multiple-`with` surface sugar now LANDED** (`3babfdc`, PARSER-only, no elab/core): `with e1 e2` + comma-separated arm patterns `p1, p2 -> body` desugar to nested single-`with` via `collect_with_scrutinees` + `build_multi_with` (groups arms by first pattern using structural equality, first-appearance order). Oracle `withmulti/wm01_two_with` accept/accept, `wm04_two_with_neg` reject/reject (soundness flows through the desugar). First-slice constraints, each a clean parse error: grouping requires structural equality of shared-head outer patterns (`with_multi_inconsistent_pattern` — variable renaming deferred); LHS-rematch+multiple-`with`, `proof`+multiple-`with`, and arity mismatch are out of scope. Full suite 2933/0. **Named/dependent ctor args now LANDED** (P + E, oracle `withctor/wc01_dep_ctor_args` accept/accept, `wc02_dep_ctor_neg` reject/reject): a constructor signature may carry a NAMED explicit binder `(k: Nat)` so a later argument type or the result index can depend on it — `vc : (k: Nat) -> SNat(k) -> NVv(S(k))`. Parser: `parse_ctor_dom` recognises `( ident : … )` at an arrow-chain domain position and yields `{:named_dom, name, inner}` (unnamed args unchanged byte-for-byte). Elaborator (`elaborate_gadt_ctor`): a `{:named_dom,…}` domain uses its declared name in the ctor telescope and threads it into `full_scope`, so subsequent domains AND the result indices resolve it as a de Bruijn binder; the name is excluded from implicit inference (it is a runtime arg, `:present`). Retires the singleton-`SNat`-carrying workaround. Additionally, the no-eq (no proof, no sibling) `with` path is now index-agnostic — a bare `with v` over an indexed-family scrutinee routes to `elaborate_match` (which already refines indexed families), completing the subsumption the code documented; the `need_eq` indexed case still requires the LHS-rematch form. No TCB. NOT covered: computed (non-invertible) result indices like `NVv(add(k,k))` (stuck inversion, Idris shares); named binders only in ctor arrow-chain domains (not general type-atom positions). | E, P | additive | ✅ (A + B + sibling + indexed LHS-rematch + **nested `with`** + **multiple-`with` surface** + **named/dependent ctor args**; reach: #5/arithmetic-restated view/codegen) |
| 7 | Propositional equality | Automatic `rewrite` motive inference (abstract LHS occurrences in the goal, à la Idris `rewrite … in`) — implemented in `rewrite_plan/6`, audited to parity with `elabRewrite` (P0); motive-under-`:case`-binder capture bug fixed. Conversion-occurrence rewriting (oracle probe rw07) now **parity** (`same`) via an elaborator bridge-lemma step (`2ac4add`): a refl-bodied bridge checked at the asymmetric endpoint through a constant motive, so the kernel only ever decides a top-level conversion. **Multi-occurrence rewriting now parity** (oracle `rw08` syntactic-multi + `rw09` conversion-multi, both `same`; `frp08_interchange_multi` `same`) via the LAZY-UNFOLDING TCB fix in TCB note (3) below: a stuck recursive call now has ONE canonical (folded) shape everywhere, so the single-pass syntactic occurrence-abstraction in `rewrite_plan/6` finds all occurrences uniformly — no elaborator change needed **GRADUATED — identity type fully inductive (Phase B/C complete, 2026-07-08):** `Equivalent : (a:Type) -> a -> a -> Type` with sole erased-witness ctor `reflexive` is the ONLY identity representation; `rewrite` desugars to the standard J/subst transport — `{:app, {:case, proof, λx y p. (M@x) -> (M@y), [reflexive(w) -> λh. h]}, body}` (the canonical Agda/Lean derivation of subst from J; spec 2026-07-04 'Phase-B encoding amendment') — with the body checked OUTSIDE at `M@a`, so `rewrite_plan`'s occurrence gate and verdicts are unchanged. The primitive `{:eq}`/`{:refl}`/`{:rewrite}`/`{:veq}`/`{:vrefl}` Core forms are RETIRED (kernel/eval/quote/term/serializer clauses stripped; validator rejects smuggled nodes at dev time for eq/refl and at release for rewrite). Proofs are runtime-free via collapsible-family erasure (single all-erased-fields ctor ⇒ the transport case erases to its body — Brady/McBride/McKinna). K/UIP stance UNCHANGED: definitional K via the index unifier, operator-signed-off 2026-07-04 (builtins.ex doc); refl-matching discharges/refines exactly per `branch_unify` and equates no distinct normal forms (Gate C antibody). `bridge_step` deleted as dead code (lazy δ-unfolding + nf idempotence make its firing condition unsatisfiable; rw07/rw09 route through `contains_a`). | E | additive | ✅ |
| 8 | Equality / absurdity | `Void`/absurd elimination at the surface (`{:absurd}`) | K (leaf), E | additive | ✅ |
| 9 | Inference unification | First-order metavariable engine: alloc, occurs-checked solve, zonk (`lib/cure/elab/unify.ex`) | E | — | ✅ |
| 10 | Inference unification | Pattern (Miller) unification — solve `?m x y = t`. **Landed** (oracle `dep/dep07_higher_order_family` accept/accept, adversarial neg `dep/dep09_higher_order_family_neg` reject/reject): inferring an implicit type family `{F: (N) -> Type}` from a lambda argument's type (`the2(fn(n)->n, m)`). Four additive E-layer links, no TCB: (1) the **Miller solver** `Unify.miller_solve` (`e419f09`) solves `?m x̄ := λx̄. t` when a metavariable is applied to distinct bound variables and the other side is rigid — `t` abstracted over `x̄` (de Bruijn: pattern var → solution-lambda binder, ambient var shifted, non-pattern crossed binder escapes), domains from the metavariable's recorded type (`MetaCtx.fresh/2` now stores types, threaded at the implicit-creation sites); conservative — any condition unmet falls through to first-order. (2) **Dependent function-type syntax** `(x: A) -> B(x)` (`ff314ad`, P + E): parser captures an optional `name:` binder per parenthesised domain, emitting `{:pi_type, …}` when any domain is named (else the existing non-dependent `Function` verbatim); `idx_to_core` builds the Core Π threading each binder into scope, mirroring the `sigma_type` clause (oracle `dep/dep08_dependent_arrow_param` accept/accept, isolates the parser feature). (3) **Scoped applied head** (`idx_to_core`): an applied name resolves against the de Bruijn scope before the family/ctor/global fallback, so `F(n)` (F an implicit param in scope) becomes `{:app, {:var,i}, …}` not a dangling `{:global, :F}` — the real first blocker, since as a global the call-site implicit substitution could never make `F` a solvable metavariable. (4) **Lambda-at-meta-Π solve** (`try_lambda_meta_pi`): a lambda arg at `(n:N) -> ?F(n)` binds the parameter, infers the body, and unifies the reconstructed Π against the expected one so the codomain metavariable solves under the binder — additive/fallback-only, reached only after inference failed, kernel re-checked. *Reach*: the ambient-variable Miller case via a non-lambda argument (`subst(e, v)`) is a separate path, still cleanly rejected (no-crash test green) | E, P | additive | ✅ |
| 11 | Inference unification | **✅ LANDED (2026-07-03) — whnf-before-compare** (E-layer, no TCB): the Task-1 risk gate + a two-round Idris/Agda/Lean cross-read showed the reachable inference gap is NOT postponement but **missing weak-head normalization before structural unification**. Cure's unifier compared un-normalized terms, so a computed index like `Vec(plus(Z, ?m))` failed (`{:app,…}` vs `{:ctor,…}`) instead of reducing `plus(Z, ?m)` → `?m` and solving `?m := S(Z)`. **Fix:** `Unify.whnf_pre` weak-head-normalises both sides (meta-aware — unsolved metavariables become reserved opaque-global placeholders, reusing the **trusted** `Normalise.whnf` unchanged) before `do_unify`; gated to CLOSED terms at the ambient frame (depth 0), applied ONCE per step (the structural descent re-whnfs subterms → no fold/unfold oscillation), with a reduction that turns a non-lambda into a lambda discarded so under-applied spines (`app(xs) =? app(?a)`) still unify first-order. All three references do reduce-before-compare as the intrinsic PRIMARY mechanism (Idris `nf` Unify.idr:1300-1302; Agda `reduceB` Conversion.hs:586-587; Lean `whnfCoreAtDefEq` ExprDefEq.lean:2327-2330); postponement is their *separate secondary* mechanism, unreachable-to-a-verdict-flip from surface Cure, so **DEFERRED**. Probes: `whnf01_computed_index` + `whnf02_two_arg_shared_index` accept/accept (the flips); `whnf03_stuck_meta_neg` (genuinely stuck `plus(?m, Z)`) + `whnf04_concrete_mismatch_neg` (reduce-then-mismatch) reject/reject (guards). Spec `2026-07-03-whnf-unification-design.md`, plan `…-whnf-unification-plan.md`. The postponement design (A/B/C) below is retained as a DEFERRED follow-up. — Postponed/suspended constraints (real flex-flex: constraint queue + retry-on-progress). **Reference design: Abel & Pientka, "Extensions to Miller's Pattern Unification for Dependent Types and Records," MSCS 2018** (`~/Downloads/unif_miller60.pdf`; extended version of the ICFP/2011 paper) — the canonical treatment of the *dynamic* pattern fragment, i.e. exactly this row. Cure-specific framing: **all of this is E-layer/untrusted** — the kernel re-checks every elaborated term, so the paper's type-/solution-preservation proofs are a *completeness* concern for us, not soundness. That makes it a **menu, not a mandate** (adopt any subset with zero TCB exposure) AND materially simplifies our version. Prioritised design when this is picked up: **(A) postponement / constraint-set inference system** (paper §3): model unification as a set of constraints refined by small steps; when a constraint is not yet in the pattern fragment, POSTPONE and retry once other metavars are solved (so it later falls into the fragment). §3.1 **"typing modulo constraints"** is the specifically-*dependent* subtlety — a constraint's well-typedness can depend on another constraint's solution — but Cure's **kernel re-check lets us skip the typing-modulo bookkeeping the paper needs** (we tolerate ill-typed intermediate constraints; the kernel is the backstop). **(B) pruning + strongly-rigid occurs-check** (paper §3.4, Fig 5, and the p.12 occurs-check): pruning (remove bound-var dependencies that cannot be part of a solution) is what makes postponed/flex-rigid constraints actually solvable; and the occurs-check refinement fixes a latent completeness gap — our current `Unify.occurs?` rejects ANY occurrence of `u` in its solution, but only **strongly-rigid** recursive occurrences are truly unsolvable (weakly-rigid ones, under another metavar or in an eliminable position, ARE solvable). Our naive check is **sound but over-rejects** (costs reach, never soundness — consistent with the conservative-fallthrough Miller in #10); refine it here, not before. **(C) Σ-flattening / projection elimination** (Figs 3-4): a metavar applied to *projections* (`?m (fst y) (snd y)`) is translated into pattern form via the Σ≅Π+unit isomorphism — Cure HAS Σ/records (#4) and #10's solver punts on projected args, so this is the technique to close that; do it **on demand** when a projection-inference oracle probe actually fails. **Skip:** the contextual-metavariable representation (`u[σ]` closures / metavar-as-object-in-context) — a rewrite of our `{:meta,id}`+`{:app}` model for "logical grounding," not capability; the one useful ingredient (metavar carries its type) already landed in #10 (`MetaCtx.fresh/2`). Verify like #10: oracle probes against `idris2`, E-layer, no TCB. **✅ SIMULTANEOUS UNIFICATION — landed (2026-07-19), one scoped piece of design (A) postponement** (E-layer, no TCB): a constructor whose result carries a COMPUTED index, applied inline at a goal that ALSO carries an implicit the present arguments determine — `ATimes : Acc(l,m1) -> Acc(r,m2) -> Acc(PTimes(l,r), add(m1,m2))` checked against `Acc(PTimes(PA(a),PStar(PA(a))), S(Z))` with `a` implicit. The deferred-domain solver used all-or-nothing whole-tuple `Unify.unify`, so the STUCK `add(m1,m2)` component (its scrutinee an unsolved seed meta) aborted every OTHER component and the sibling implicit `a` — which the structural first index + the present field `AAtomA : …PA(TA)…` jointly determine — never solved → spurious `:unsolved_metavariables`. **Fix:** unify the data template COMPONENT-WISE (`unify_data_components` in `solve_deferred_domain`; `partial_pin_result` + `retry_deferred` on the checked-ctor fallback), keeping the solutions of the components that unify and DEFERRING the stuck ones into the field-resolution fixpoint (`resolve_ctor_fields`) — once a present field pins `m1`/`m2` the computed index reduces (`add(S(Z),m2)` → `S(m2)` via the meta-aware whnf) and the deferred equation `add(m1,m2)=S(Z)` retries green. Idris's simultaneous-unification behaviour scoped to one constructor: field checks and result-index constraints solve as ONE constraint set with postponement; the assembled term is kernel-re-checked, so a stuck-then-unsatisfiable component surfaces as an unsolved metavar or a kernel index mismatch, never silent acceptance. Oracle `e6simuni/e6simuni01_computed_index_implicit_goal` accept/accept + boundary `e6simuni02_wrong_measure_rejects` reject/reject; antibody `test/antigen/simultaneous_unification_antibody_test.exs` (REACH + measure-checked + no-fabrication). **Reach-pinned residual (variant A):** the same computed-index constructor at a CONCRETE goal (`star_fold(x: Acc(PTimes(PA(TA),PStar(PA(TA))), S(Z)))`, no implicit) still fails `:ctor_arity` — a SEPARATE params/indices-split bug in signature type-lowering (`declarations.ex`), NOT the sim-uni gap. It is what blocks converting `Otp.Meta.MailboxPattern`'s explicit `(m1: MS) -> (m2: MS)` `ATimes`/`AStarN` indices to relevant implicits (its construction sites build at concrete goals), so that workaround-removal waits on variant A | E | additive | ✅ |
| 12 | Totality — termination | Single-argument structural descent | E | — | ✅ |
| 13 | Totality — termination | **Mutual recursion**: soundness hole closed (`d13d718`; `diverging_mutual_pair` replays `:ok`, antibody banked as a permanent regression guard). Remaining is *reach* — well-founded mutual / lexicographic descent is conservatively rejected, not unsoundly accepted (P1/#14) | K, E | reach | ✅ (REACH LANDED `88452193` — cross-function/mutual size-change, port of Idris `SizeChange.idr` `addFunctions`: SCC group, non-square cross-edges, multi-function closure, certify iff every idempotent endo-edge has a `:smaller` diagonal. Replaces the `mutually_recursive?/3` blanket reject; `d13d718` diverging-pair antibody now the primary soundness witness. Full gate: soundness review + full suite 2817/0 + Idris cross-check [even/odd + ping/pong certify = Idris total; diverging pair rejects = Idris non-total]) |
| 14 | Totality — termination | Multi-argument / lexicographic (size-change) descent. **Empirically characterised (not oracle-measurable): the elaborator already ACCEPTS a lexicographically-terminating multi-argument function** (`f(a,b)`: `f(m, Z())` decreases arg 1, `f(S(m), n)` decreases arg 2 — no single argument decreases in every call), so there is no accept/reject *reach* the differential oracle can pin — both Cure and Idris accept. The gap is **assurance, not reach**: Cure elaborates such a function *uncertified* (single-argument structural descent, #12, cannot prove it total), whereas Idris/Agda **certify** it total via size-change / lexicographic ordering. Closing it is a change to the soundness-critical totality certificate (`certificate.ex`, **TCB** — the "total" guarantee rests here), tested via **Antigen** (a size-change antibody: the lexicographic function certifies total AND a genuinely non-total multi-arg control still rejects), not via an oracle probe. So this is a reviewed TCB run with the full gate (red-green + antibody + full Antigen + full suite), aligned with Idris `Termination/SizeChange.idr` / Agda `Termination/*` — **not** an E-layer additive tick as previously tagged | K (certificate), E | assurance (TCB, size-change) | ✅ (LANDED `b871b37` — single-function size-change + reconstruct-equal, port of Idris `SizeChange.idr`/`CallGraph.idr` `sizeEq s t => Same`; full TCB gate: soundness review + full Antigen + full suite 2804/0 + Idris cross-check both directions [Ackermann/swap/lex/cov certify total, `loop` control rejected]; size-change antibody banked. Cross-function/mutual is #13's reach tail) |
| 15 | Totality — coverage | Kernel exhaustiveness (`declared ⊆ covered`) | K | — | ✅ |
| 16 | Totality — coverage | Surface exhaustiveness diagnostics accounting for discharged/impossible branches | E | additive | ✅ |
| 17 | Totality — coverage | Coverage over nested patterns | E | additive | ✅ (via #3: nested patterns lower to nested single-level matches, each coverage-checked by the kernel, so an uncovered sub-case surfaces as a genuine `{:missing_branch, ctor}` at any depth — oracle `match/mt10_nested_nonexhaustive` reject/reject, exhaustive `match/mt07` accept/accept) |
| 18 | Positivity | Strict positivity checker + vertical | K/E, A | — | ✅ |
| 19 | Positivity | Confirm + bank nested / through-constructor / negative-position (arrow-left) positivity | K/E, A | verify + additive | ✅ (W4: audited, holes fixed, antibodies banked) |
| 20 | Universes | Cumulative universes, no `Type:Type`, two-universe constructor-field rule | K | — | ✅ |
| 21 | Assurance (meta) | Known-label regression net across all verticals | A | — | ✅ |
| 22 | Assurance (meta) | **Term-generator metatheory engine** (StreamData-backed corpus, known-label totality) — turns the net into soundness *evidence*. **Tier B landed** (`2026-07-02-antigen-tier-b-term-generator` spec+plan): mode-directed dependent `gen_term(Γ,T)` over a versioned signature menu + dependent context generator, three differential self-consistency assays (`term/infer_check`, `term/subject_reduction`, `term/normalization`), and a binder-usage/reduction-activity health gate. Acceptance run: 0 infections over the generated stream, `→ healthy`. *Reach still open*: `conversion_termination`/`erasure_preservation` assays, ill-typed mutation corpus, `ChoiceSeq` backend, and a richer menu (Pi/Sigma goals, type parameters) — all deferred to a follow-up spec | A | additive | ✅ |
| 23 | Assurance (meta) | Missing per-rule antibodies: occurs-check/cycle and deletion rule | A | additive | ✅ (W3: deletion antibody + occurs pin) |
| 24 | Assurance (meta) | Wire a forced/dot-pattern (`forcing`) Antigen vertical once #5 exists | A | additive | ✅ (#5 landed; vertical BUILT and green — `Antigen.Assays.DotForcing` (`forcing/dot`, the known-label oracle that drives the exact forced-annotation check via `Elaborator.forced_check_probe/7` → `named_implicit_forced_value/4` + `Conv.conv?/5`, returning `:accept`/`:reject`/`:unforced` against a correct-by-construction label) + `Antigen.Generators.{DotForcing,ElabDotForcing}` + a dedicated `:dot_forcing` challenge kind, all registered in the runner and cover-manifest. Regression-guards the shared forced-value/convertibility primitives under carried-shaped (multi-index, multi-sibling) substs; the carried-eq motive branch's own forced check is additionally covered end-to-end by `test/cure/elab/named_implicit_tail_test.exs` + the `ElabDotForcing` catalog. Green: `elab_dot_forcing`/`dot_forcing`/`forced_check_probe` tests 22/0, cover-manifest gate 318/318 cells across 31 assays. Scope note: an Antigen (K/E-soundness) vertical, so it does NOT cover the surface dot-syntax *parse* (852742a, P-layer) — correct scope for this row) |
| 25 | Assurance (meta) | Surface-level `.cure` regression corpus for the ④ features | A | additive | ✅ (largely built incrementally — 137 committed `.cure`↔`.idr` oracle probes across 23 clusters, all differential-vs-idris2 and `oracle_replay_test.exs`-guarded. The ④ surface features had *unit* tests but no *differential* probes; now filled by the `impossible` cluster: `imp01_explicit_impossible` (`-> impossible` on an unreachable indexed ctor) accept/accept, `imp02_omit_impossible` (omitted impossible ctor auto-discharged via `{:absurd}` — exercises row 8's surface absurd path) accept/accept, `imp03_reachable_impossible_neg` (a REACHABLE ctor marked `-> impossible`) reject/reject. Row-16 exhaustiveness-with-discharge is covered by `match/mt04`/`mt10` + `imp03`. Optional polish: a CORPUS.md manifest mapping each ④ feature → cluster) |
| 26 | Dependent matching | Expression-level / inline `match` (Idris `Elab/Case.idr`). **Checking mode landed** (`fcdf5ce`): a `match` in nested expression position — as a `rewrite … in` body and as an arm body of another `match`, non-dependent and with a dependent goal that mentions the matched variable (motive refines per level); non-exhaustive nested matches now give the real `{:missing_branch,_}` coverage error. **Let-blocks now land** (the earlier `cure_stricter` on `mt05_let_tail_reach` is CLOSED — the probe is now `same`, accept/accept): the dependent elaborator's `{:block, …}` clause (`elaborator.ex` `elaborate_let_block`) lowers a `let x = e ⏎ body` to a β-redex `(λ x:T. body) e` in checking mode, so `let r = match n … ⏎ r` type-checks. **ML-style `let <pat> = <value> in <body>`** also lands (oracle `letin/li01_let_in_body` + `letin/li02_let_in_argument`, both accept/accept): `parse_let` consumes an optional trailing `in <body>` and desugars to the very same two-statement `{:block, …}` node, so `let … in …` works in any expression position (function argument, if-branch) — surface ergonomics, no new reach, block-`let` byte-for-byte unchanged. *Reach*: only inference-position inline match remains (a bare `match` whose type must be synthesised with no expected type → Idris's auxiliary case-function lift), and it is awkward to even express in Cure's indentation-based surface. **Inference-position `match` now LANDED (`d629af1`, non-dependent case)** — `elaborate_expr_typed` had no `:pattern_match` clause, so a `match` with no expected type (a nested-match scrutinee, or a `let`-bound match — `let` desugars by *surface substitution*, pushing the match into the scrutinee/inference slot) fell to `:unsupported_expression`. Fixed by a new `elaborate_expr_typed({:pattern_match,…})` clause that SYNTHESISES the result type `T` from the first constructor arm's body (infer in the arm's branch context, `Quote.reify`, then `strengthen_inferred_type` out of the branch frame via an `occurs_below?`-guarded `Subst.shift(_,-arity,0)`), then reuses the checked `elaborate_match(scrut, arms, T, …)` so every arm is checked against the one `T`. Oracle `infmatch/im02_let_equiv` accept/accept (the `let`-form now works); `im03_incompatible_arms` reject/reject (soundness — arms with distinct types rejected by both); `im04_dependent_needs_annotation` reject/reject (a genuinely dependent inference-position match — arm type mentions a constructor-bound var — errors `:cannot_infer_dependent_match`, and Idris likewise rejects the un-annotated form). E-layer, no TCB; full suite 2930/0, replay 41/41. *Residual*: (a) dependent inference-position match needs an explicit annotation (faithful — Idris requires it too); (b) nested inference-position `match` (`im01`, `match (match …) …`) is **at parity** — oracle `infmatch/im01_nested_inference_match` accept/accept: the inner match sits in the outer's scrutinee (inference) slot and reuses the `d629af1` inference-position clause. The one spelling that does NOT parse is a *multi-line indented* inner match inside parens, because the lexer suppresses newlines under `paren_depth > 0` (Python-style implicit continuation, `lexer.ex:368`) so no `:indent` is emitted — a deliberate surface consequence, not a reach gap: the inner match is authored with the inline **brace** form `match n { … }` (Idris likewise uses `case … of { … }` for inline layout), which parses and elaborates inside parens. **`let`-duplication caveat partially CLOSED** (`809823d`): a `let x = e` whose binder is later SHADOWED by a pattern var was rejected (`:let_shadowed_binder`) because `elaborate_let_block` refines by surface substitution (which would capture); Idris accepts it. Fixed in the shadowing branch only by the bind-once β-redex `(λ x:T. <rest>) e` (the [[bind-once-beta-redex-finding]] mechanism, same as #4's guarded scrutinee) — a real de-Bruijn binder a deeper pattern binder correctly shadows. Oracle `letbind/lb01_shadowed_let` reject→accept, `lb02` neg reject/reject; non-shadowing `letin` path unchanged; full suite 2943/0. Residual: bind-once for the non-shadowing MULTI-USE case is pure efficiency (Cure is total, so duplication changes no value), and shadowing + a genuinely-dependent `let` stays on substitution | E, P | additive | ✅ (checked + let-blocks + `let…in` + **non-dependent inference-position `match`** + **nested inference-position `match`** (`infmatch/im01`, accept/accept) + **bind-once shadowed `let`**; no reach/soundness gap remains — residuals are all beyond-scope or faithful: dependent inference-position match needs an explicit annotation (FAITHFUL — Idris requires it too, `im04` reject/reject), multi-line *indented* inner match inside parens is unauthorable by the implicit-continuation lexer design but the inline **brace** form is at parity, and non-shadowing let-duplication is efficiency-only (Cure is total, so duplication changes no value)) |
| 27 | First-class functions | Function types, lambdas, higher-order functions. **Landed** (oracle `func/fn01_higher_order` + `func/fn02_lambda_body`, accept/accept): (a) a non-dependent arrow `(A)->B` (surface `Function(A,…,R)`, `function_type`) now elaborates to a native Core Π `Π(_:A). … R` (`declarations.ex` `arrow_to_pi`, non-dependent shift) — previously a meaningless `{:app, Function, …}`, so applying a function-typed param already failed; (b) a lambda `fn(y) -> body` is elaborated in **checking mode** against the expected Π (`elaborator.ex` `elaborate_lambda`, currying multi-param), routed from a lambda function-body (`declarations.ex` `elaborate_body`); (c) codegen (`emit.ex`) erases a lambda to a curried 1-arg BEAM fun, applies a closure one arg at a time, and passes a named function as `fun name/arity`. **BEAM-verified end-to-end**: `ap(inc, S(Z())) = S(S(Z))` fully in Cure; lambda values, closure application, currying all run. No TCB (kernel already had Π + `{:lam}` check). **Lambda-as-argument now lands too** (oracle `func/fn03_lambda_argument`, accept/accept): a call carrying a lambda argument is elaborated by `elaborate_bidirectional_app` — checked argument-by-argument against the callee's Π telescope (codomain instantiated at each argument's value, so dependent parameters are honoured), so the untyped lambda gets its parameter type from the domain. Restricted to lambda-bearing calls, so every other application keeps its exact inference path; BEAM-verified (`ap(fn(y)->S(y), n) = S(Z)`). **Chained application** of a call result also lands (oracle `func/fn04_chained_application`, accept/accept): `f(x)(y)` parses with the inner call preserved as a `:callee` meta — the elaborator applies the outer arguments to the callee's function-typed result (`check_app_args`), codegen calls the callee then curries the extra arguments onto the returned fun (`emit.ex` splits a global-head application at the function's arity), and erase pads its quantity list so over-applied arguments are kept as present. BEAM-verified `mk()(z) = S(Z)`, `adder()(a)(b)`. **Complete** for the non-inference surface | E, P, C | additive | ✅ (types + lambdas + HOFs + closure codegen + chained application; only whole-program Miller inference for un-annotated HOF composition is beyond scope) |
| 28 | Infer/check coherence (kernel) | **Infer/check coherence on params-on-spine constructors** (batch task #14, spec `2026-07-09-infer-check-coherence-design`): `check`'s ctor clause hard-coded the fields-only telescope and returned `:ctor_arity` for the K6 params-on-spine spelling *before* any semantic judgement, so `infer(t)={:ok,A} ⇏ check(t,A)=:ok` — the coherence property every reference kernel guarantees. Fixed by an ordered `cond` in `check/3` routing the `pc + fields` arity to a shared `check_via_infer/3` (infer + conv), Lean-aligned (`check = infer + is_def_eq`); the fields-only path stays byte-identical and the branch order is load-bearing (`pc==0` collapses spine≡fields-only). Antigen equality generator widened past the (now-historical) coherent-fragment restriction — a top-level bare spine reflexive and a checking-position embedding, patrolled by the existing `term/infer_check` + `term/subject_reduction` assays — plus a deterministic `spine_ctor_coherence_antibody` pin. **Sibling finding RESOLVED (fields-only landed `7b7f071`, ledger #28) — value-level ctor-spelling dichotomy:** the two spellings also diverge *below* the typing judgement — spine vs fields-only `vctor`s are NOT definitionally equal (`Conv` compares spines length-strictly, `conv.ex:88-89`, `:183-186`); case ι-reduction over a spine-form scrutinee mis-binds OPEN branch bodies, every ambient de Bruijn ref shifted by `pc` (`eval.ex:38`, `:56-62`); `Erase` keeps the spine params (`erase.ex:20-36`). All pre-existing and reachable through pure `infer` today; the coherence fix changes their *incidence*, not their existence. Resolution = ONE canonical spelling; the operator approved **fields-only** (Agda-aligned) conditional on "we lose nothing", and the 2026-07-09 kernel audit PROVED the condition (verdict FIELDS-ONLY SAFE under the shared-type conversion invariant: `Conv` is entered only on same-type pairs and reaches a `vctor` comparison only after the enclosing `vdata` params converted, so a mixed spelling can only false-REJECT via a length gate, never false-ACCEPT). Landed `7b7f071` (task #22): A1 ι-rule coercion `Eval.drop_leading_params` at every ι site (closes the K6 over-long-`vctor` de Bruijn-misalignment hazard), A2 `conv.ex:coerce_fields` conversion completeness, A4 antibodies `test/antigen/ctor_spelling_antibody_test.exs` (params-on-spine ι ≡ fields-only result; `Conv` equates the two spellings; nf/eval never bind more args than branch arity). Fields-only chosen over Lean params-always as the strictly smaller trusted surface — the kernel was already fields-only at every load-bearing site (ι, erase grade-0 drop, emit tag+fields), so the change codified reality rather than threading params through `Conv`/`Eval`/`Erase`. Spec `2026-07-09-canonical-spelling-design`, memory note `ctor-spelling-value-dichotomy`. | K, A | assurance (TCB) + resolved fork | ✅ coherence fix landed (`f7fd3e0` kernel restructure + `603d541` Antigen widening/pin) + sibling ctor-spelling value dichotomy RESOLVED fields-only (`7b7f071`, audited FIELDS-ONLY SAFE, antibody-gated `ctor_spelling_antibody_test.exs`) |

### Already at parity — no work (bounds the scope)
Rows **1, 9, 12, 15, 18, 20**: the kernel type checker, first-order index
unifier, universes, structural termination, coverage, and positivity are at
Idris quality for what they cover. **The TCB is essentially done** — with #8's
`{:absurd}` leaf landed, #13's mutual-recursion hole closed, and #19's
nested-positivity gaps audited + banked, every remaining row lives in the
untrusted elaborator / Antigen layers.

**Two known K-layer *reaches* (soundness-safe incompleteness): BOTH now CLOSED
via reviewed TCB runs.**

*(1) Stuck-eliminator normalization — ✅ CLOSED (`d37721f`, reviewed TCB run).*
The normalizer used to preserve stuck eliminators without δ-reducing their
targets: a stuck `case` never re-reduced its scrutinee, and `spine/2` only
unwrapped `napp`, so `nfst`/`nsnd` frames over a certified-global spine froze —
`nf(plus(plus(Z,n),Z))` and `fst(g(x))` stayed stuck even at top level. Fixed by
a single seam clause in `unfold_certified_head` (`normalise.ex`): when the spine
head is a stuck `ncase`/`nfst`/`nsnd`, whnf its target (threading `opts`, so
`delta: :none`/fuel honored) and, if a ctor/pair emerges, apply the same ι-rule
`eval` trusts, then re-apply the spine args. Conversion inherited the fix via
`conv_val?`'s existing `whnf_value` routing (`conv.ex` untouched). Gate met:
red-green kernel tests, the new `StuckElimDelta` Antigen antibody (termination +
soundness, with a banked negative control that must stay distinct), full Antigen
suite (106), full test suite (2256, 0 regressions). **Correction to the earlier
plan:** this does NOT retire rw07's bridge lemma. The seam reduces
`plus(plus(Z,n),Z)` to `plus(n,Z)` (inner stuck `plus(Z,n)→n`), but `plus(n,Z) ≡
n` is *genuinely not definitional* (needs induction / `plus_n_Z`), so
`conv?(plus(plus(Z,n),Z), n)` is correctly still `false` and the bridge remains
necessary for that rewrite. What the seam fixes is the real reduction gap
(stuck-δ-scrutinee `case` + `fst`/`snd` over certified-global spines), not any
equality requiring induction.

*(2) `check_motive_wf` reify-collapse — ✅ CLOSED (`defc6cb`, reviewed TCB run).*
`Quote.reify` collapses `{:vdata, name, args}` → `{:data, name, args, []}` (no
inductive sig to recover the param/index split). This was invisible except in
`infer_type_value_sort`, which inferred a Π/Σ/Eq motive body's sort by reifying +
re-inferring — so an **indexed family as a Π domain** (the *convoy* encoding of
`with` sibling refinement, `λw. Π(SNat(w)). …`) re-inferred with `:arg_arity` and
was wrongly rejected `:bad_motive`. **Fixed without touching `reify`** (approach
b, the smaller/safer of the two — `reify` feeds 11+ callers): the `{:vpi}`/
`{:vsigma}`/`{:veq}` clauses of `infer_type_value_sort` now recurse on the
sub-*values* directly, mirroring `infer/2`'s type-formation rules and bottoming
out in the existing direct `{:vdata,…}` clause — so acceptance equals a non-lossy
reify+infer, and a non-type domain still falls to `:not_a_type_value` (rejected;
proven by a banked negative control). `reify`/`conv`/`normalise`/elaborator
untouched. Gate met: red-green kernel test (positive accepted + negative control
rejected), Antigen `indexed/case` antibodies (both banked), full Antigen (106),
full suite (2258, 0 regressions). This unblocks the clean **convoy** encoding and
is a prerequisite for indexed-scrutinee `with` / views. The elaborator's
`resplit_data` workaround is now redundant-but-harmless (leave for a follow-up).
**Residual — ✅ CLOSED (B3, reviewed TCB run; signature-aware `Quote.reify`).**
The `{:veq}` clause value-recurses on the Eq *carrier* but used to **reify the Eq
endpoints** flatly — so an `Eq` whose endpoints are indexed-family TYPE values
(e.g. `Eq(Type, SNat(x), SNat(x))` as a motive body) was false-rejected
`:bad_motive` via the split-collapse. Fixed with the **principled repair**
(Agda `getNumberOfParameters` / Lean `inductive_val.get_nparams`): `Quote.reify`
takes an optional `sig` (default `nil` = flat read-back, unchanged for all 44
external call sites); when passed, the `{:vdata}` clause recovers the
param/index split from the family's parameter-telescope length. The lone caller
threading `sig` is `infer_type_value_sort`'s `{:veq}` endpoint check, so
`check` sees the correct arity. Conversion stays flat (untouched), so no distinct
normal forms are equated — the collapse only ever caused a false *rejection*,
never a false acceptance, and the fix removes it without widening equality. Gate:
red→green `motive_wf_indexed_eq_endpoint_test` (positive accept + negative control
still rejects an endpoint that does not inhabit the carrier), reach pin
`reach_reify_split.sexp` migrated byte-identically into `corpus.sexp` and deleted
(same commit), full Antigen (192), full suite (2569), independent adversarial
verification. The indexed with-rematch convoy (`elaborate_with_rematch`) remains
a sound non-TCB path; this closes the underlying reach it worked around.

*(3) Lazy unfolding of stuck recursive globals — ✅ CLOSED (reviewed TCB run).*
The exact DUAL of note (1). Note (1) makes a stuck eliminator whose target
δ-reduces to a ctor/pair FIRE; this note stops the *unproductive* direction from
over-expanding. The normalizer used to unconditionally δ-unfold a certified
global, so `plus n Z` (n neutral) expanded into its internal `case n {…}` even
though that `case` is itself stuck — leaving the SAME stuck call with two shapes
(folded `plus n Z` in one spine, expanded `case n {…}` in another). That
δ-inconsistent normal form broke the elaborator's syntactic occurrence-matching
(the `rw08` hang: `conv?` δ-looped on the mis-built motive) and is exactly the
non-canonicality Idris/Lean/Agda avoid by **lazy unfolding** — a recursive
definition stuck on a neutral stays an opaque atom, never expanded into its
matcher. Fixed by a single guard in `unfold_certified_head` (`normalise.ex`):
after computing the unfold, keep the application FOLDED (`:stuck`) when the
result is a neutral whose spine head is an eliminator (`ncase`/`nfst`/`nsnd`)
whose target does not whnf to a ctor/pair (`productive_unfold?`). Sound by
construction: freezing only makes normal forms MORE distinct (never collapses
two), and `conv?` still δ-unfolds on demand — so `conv?(plus(n,Z), n)` stays
correctly `false` (consistent with note (1); rw07's bridge unaffected) while
`plus(Z,n) ≡ n` and `plus(S Z,Z) ≡ S Z` still fire (no over-freezing). Gate met:
red→green `normalise_test` (the `step`-under-neutral characterization updated to
the folded stuck form — its "stays stuck" invariant preserved, not weakened),
the new `LazyUnfold` A6 antibody (`test/antigen/lazy_unfold_antibody_test.exs`:
termination + canonical-form convergence + no distinct-NF collapse + no
over-freezing), full Antigen (192), full suite (2567, 0 regressions),
independent adversarial verification. Closes the "K-layer reach" that note (1)
and row #7 had tracked as still-open for multi-occurrence.

### The honest headline
Of 26 rows: **17 at parity, 9 remain, 0 live soundness holes** (row #10 Miller
pattern unification landed — higher-order implicit inference + dependent
function-type syntax, oracle dep07/dep08/dep09) — the
transliteration-P0 audit landed ④'s rows 2/8/16 and #7's audited-complete
`rewrite` motive inference (rw07 now closed via the elaborator bridge lemma),
the pre-port banking run closed #13's mutual-recursion hole (now a reach item),
#19's nested positivity, and #23's missing antibodies, the post-merge port
run landed checked-mode expression-level `match` (#26, `fcdf5ce`) and
`with`-abstraction capabilities A + B + sibling refinement (#6, `58037d6`,
`8487c51`, `dbf874e`), and the Antigen Tier-B run landed **#22 / A8** — the
dependent term generator plus the three differential self-consistency assays and
the health gate (acceptance run: 0 infections over the generated stream, `→
healthy`), and the parity-porting runs landed **#3** — full pattern-matrix
nested-pattern compilation (`acb22a4`/`3bd309e` + outer-catch-all weaving) — and
**#4**'s variable/wildcard catch-all (`648e852`), and — via #3 — **#17**
coverage over nested patterns (kernel-checked per lowered level). The remaining
10 are reach (#4 literals/as-patterns, #5, #6, #13, #14, #26 — several now
*partially* landed: #4 var/wildcard catch-all, #6 capabilities A + B + sibling,
#26 checked mode), ergonomics/inference (#10, #11), or assurance strength (#24,
#25, plus A10's still-open wiring of the *existing* verticals onto the generated
stream). With #3 + #17 + #22 + **#10** landed, the next-highest-leverage items are
the remaining inference-unification depth (#11 postponed constraints; #10 Miller
patterns is now at parity, incl. the dependent-arrow parser gap it surfaced) and
#5 forced patterns (dot-pattern surface now COMPLETE: bare `.e` parses + is
guarded; the expressible explicit-dot form is the named-implicit `{name = .e}`,
with positive dp02 + negative dp06 landed — bare *positional* `.e` is
structurally inexpressible for forced indices, so no further syntax is
warranted; as-patterns `x@p` already landed under #4).

## 3. Antigen — coverage and capability expansion

### 3.1 Current coverage (verified against `lib/antigen/` + `test/antigen/`)

| Vertical | Soundness property it proves | Challenges banked (well/ill) | Strength |
|---|---|---|---|
| `stub` | harness self-test (planted `{:global,:boom}`) | 1 planted infection | meta only |
| `totality/terminating` | structural-recursive defs accepted | `structural_terminating` + W2 reach pins (reach.sexp, P1 targets) | partial |
| `totality/diverging` | non-terminating defs rejected | `diverging_mutual_pair` + W1 adversarial set | solid (hole fixed `d13d718`; antibodies = permanent regression guards) |
| `positivity` | strict positivity of datatypes | positivity gen challenges + W4 escape hatches (arrow-left, double-negation, sigma-hidden, through-constructor) | strong (deep walk; double-negation already rejected; sigma-hidden + through-constructor were live holes, found and fixed D4 red-green in `6148aff`) |
| `reflexivity` (+`forcing` gen) | conversion/normalization halts (refl ≡ deep-norm; `Conv.conv_within?`, fixed 500-unfold fuel) | `forcing_pair` | solid |
| `indexed/case` | dependent-case soundness | `branch_family`, `coverage`, `refinement`, `motive_wf`, `discharge`, `injectivity` | strong |
| `rewrite/eq` | propositional equality | `eq_formation`, `refl_typing`, `rewrite_premise`, `transport_type` | strong |
| `universes` | fixed hierarchy: no Type-in-Type, ceiling, cumulativity, two-universe ctor-field rule | `type_in_type`, `ceiling`, `cumulativity`, `stratification`, `ctor_field` | strong |

Note: the `forcing` generator is **not** an unwired dot-pattern stub — it feeds
the `reflexivity`-as-normalization assay. Row 24 (a *forced/dot-pattern*
vertical) is separate future work that reuses the name only loosely.

### 3.2 Expansion ledger

| # | Expansion | Type | What it closes | Layer | Priority |
|---|---|---|---|---|---|
| A1 | Fix mutual-recursion termination so `diverging_mutual_pair` replays `:ok` | turn hole green | the one live soundness infection (ledger #13) | E + A | ✅ done (`d13d718`; antibody replays `:ok`) |
| A2 | Bank occurs-check/cycle + deletion-rule antibodies in `indexed/case` | coverage fill | kernel has both rules, neither has a named antibody (ledger #23) | A | ✅ done (deletion antibody + occurs pin banked; TCB gap in `Term` fixed en route, `360402b`) |
| A3 | Bank nested / through-constructor / negative-position positivity antibodies | coverage fill | classic positivity escape hatches (ledger #19) | A | ✅ done (three escape hatches banked; sigma-hidden + through-constructor holes found+fixed per W4 audit, `6148aff`) |
| A4 | New `universes` vertical: reject `Type:Type`, prove cumulativity sound, reject two-universe ctor-field violation | new vertical | kernel enforces universes but nothing tests them (ledger #20 has zero Antigen coverage) | A | ✅ done (universes vertical banked) |
| A5 | New `conversion`/def-eq vertical: distinct normal forms never judged equal (β/η soundness), complementing `reflexivity`'s halting check | new vertical | NbE `Conv` soundness only half-covered | A | medium |
| A6 | New `ctor-formation` vertical (or extend `indexed`): result-index shape, param uniformity, telescope well-formedness in `check_ctor` | new vertical | datatype *formation* rules under-tested | A | medium |
| A7 | New surface `.cure` vertical: elaborate through `Cure.Elab.Program` to prove ④'s `missing_branch`/`reachable_impossible`/impossible-clause behavior | new vertical | ④ currently slated for a plain regression corpus, not Antigen (ledger #25) | A + E | medium (post-④) |
| A8 | **Term-generator metatheory engine** — StreamData-backed generated corpus w/ known-label totality, swappable backend | architectural | turns every vertical from regression net into soundness *evidence* (ledger #22) | A | ✅ done (Tier B: `gen_term` + 3 differential assays + health gate; `2026-07-02-antigen-tier-b-term-generator` plan) |
| A9 | Broaden mutual-recursion challenges beyond one pair (longer cycles, indirect, guarded vs unguarded) | coverage fill | depth behind A1 | A | ✅ done (subsumed by W1 adversarial set) |
| A10 | Wire per-vertical generators into A8's generated stream once the engine lands | integration | makes A8 actually cover the verticals | A | 🟡 partial — the `:typed_term` stream feeds the three Tier-B differential assays (`term/*`); feeding the *existing* known-label verticals (totality/positivity/universes) from a generated stream remains open |

### 3.3 Shape of the Antigen work
- **A1 is closed** (`d13d718`): the checker conservatively rejects every mutual cycle and the banked antibody replays `:ok`. What remains of mutual recursion is *reach* (accepting well-founded groups — transliteration program P1), not soundness.
- **A2–A4 are cheap, high-value** — the kernel already enforces these rules; we
  bank antibodies that prove they stay enforced. A4 tests an entire kernel
  subsystem (universes) with *zero* current Antigen coverage.
- **A8 is the capability jump** — everything today is hand-built known-label
  challenges. The term generator lets Antigen claim "sound over a generated
  space" rather than "these specific cases stay fixed." A10 is its follow-on.

## 4. Explicitly out of scope (new capabilities, not parity)

These are not "quality gaps in things we support" — they are features Cure does
not have, and are excluded from this roadmap by construction:

- Linear / quantitative types (Idris 2 QTT).
- Interfaces / typeclasses at Idris depth (Cure has protocols; not the same reach).
- `%default total`/`covering`/`partial` totality modes as a language surface.
- Elaborator reflection / `%macro`.
- Named and auto-bound implicit arguments as a first-class feature.
- Nested/deep pattern compilation to a case tree **as a capability** appears as
  ledger #3 only because the kernel already nests — it is a reach item for our
  existing single-level matching, not a new domain.

### 4.1 Future / someday — deferred from the guard-coverage work

These came out of the constructor-`when` + guard-exhaustiveness design (see the
locked decision below). Recorded so they are not re-litigated; none is committed.

- **SMTCoq-style proof reconstruction** — the *only* sound way to let SMT results
  underpin dependent-checker decisions: have Z3 emit a proof and reconstruct it as
  a Core term the kernel re-checks, so the solver is a search oracle and the kernel
  stays the sole arbiter (trust nothing). Large, research-grade. The path to take
  *if* SMT-in-proofs ever becomes a goal — **not** trusting Z3's verdict directly.

- **LOCKED DECISION — do NOT wire Z3 as a *trusted* elaborator surface.** Z3 must
  never be an oracle whose "yes" the kernel relies on instead of re-deriving:
  that puts Z3 + its translator + its response parser + the subprocess into the
  soundness TCB, and unsound *dependent-coverage* (unlike a local refinement error)
  lets you inhabit `False` and collapses the whole proof theory. Refinement-tier
  Z3 trust (`lib/cure/types`) is a separate, weaker tier and does not license
  extending it into the dependent kernel. Z3 in the dependent checker is only ever
  an **untrusted lint** (can reject, never unsoundly accept) — see the guard-
  coverage work, which stays total-by-construction regardless of the solver.

- **Real `Ordering` view (`LT | EQ | GT`) + `compare : a -> a -> Ordering`** — the
  zero-trust route to "trichotomy needs no catch-all": recognize a trichotomy guard
  group and desugar to a structural `match compare(x, p)`, whose 3-constructor
  coverage the kernel checks itself (no Z3). This is how Idris earns the same
  (`with (compare x y)`). Narrower than the Z3 lint (only recognized partitions) but
  gives *kernel-real* coverage with no solver dependency. `Std.Ord.compare` today
  returns a bare `Atom` (`:lt`/`:eq`/`:gt`), so this needs the inductive `Ordering`
  first. Candidate to sit *alongside* the Z3 lint (lint for the general case, view
  for the common trichotomy).

- **Make `Bool` a real inductive (`false | true`) instead of a primitive** — worth
  investigating; *likely* net-positive. Payoff: the bespoke `bool_elim` primitive
  eliminator (added across ~6 core modules) collapses into the general `:case` on a
  2-constructor family the kernel already handles — smaller TCB, one eliminator not
  two, `if`/guards/`pickup` all desugar to ordinary `:case`, and the branch-
  comparison soundness care `bool_elim` needed is subsumed by `:case`'s tested path.
  Cost: `{:prim}` ops (`:eq`/`:lt`/…) must then *produce* the designated `Bool`
  inductive, so the kernel needs a builtin-binding tying primitive results to a
  standard inductive declaration (à la Lean/Agda builtin pragmas) — itself a TCB
  refactor. Does **not** help guard exhaustiveness. Decision rule (per operator):
  do it only if the builtin-binding cost is acceptable for the TCB-minimality win;
  do not do it for its own sake.

### 4.2 Current focus + explicit deferral (2026-07-03)

**Now building: the builtin-inductive foundation** (own spec, gated TCB effort) —
a registry letting the kernel/erasure know a canonical inductive, with **Bool as
an inductive** (retiring the `bool_elim` primitive) as first client and **Nat→Int
runtime erasure** (the Idris "Nat hack"; today `Nat` erases to unary nested tuples,
fatal on ESP32) as second. Buys Lean/Agda-class type-level TCB minimality *and*
device-viable native runtime representations — the mechanism does double duty.

Phase 2 landed (spec 2026-07-08-nat-int-erasure): canonical Std.Nat erases to
BEAM machine ints (Z→0, S→+1, case→zero-test/predecessor-bind), nominal-only
(local Nat redeclarations keep tuples); bare positive-arity constructors now
eta-expand (first-class `S`, general fix); representation agreement pinned by
the Antigen elab/nat_rep assay (kernel certified-δ nf vs BEAM).

**DEFERRED to work on the above — committed as the immediate follow-on:**
**match-embedded `when` (general)** — guards on *constructor* patterns woven into
the pattern-matrix compiler with correct fall-through, plus **Z3 as an untrusted
coverage lint** (exhaustive trichotomy drops the catch-all; non-exhaustive errors;
shadowed guard warns). The variable-pattern guard subset already landed (`92d11d5`).
This is next up once the builtin-inductive foundation lands, and it should be
*re-expressed* on whatever Bool ends up being (inductive `:case`, not `bool_elim`)
— so doing the foundation first avoids building more surface on the soon-retired
primitive. See [[smt-trust-boundary-decision]] (Z3 stays an untrusted lint).

## 5. Suggested sequencing (dependency-ordered, post-④)

1. **④ merges** (rows 2, 8, 16 land; unblocks A7/#25).
2. **A1 / #13** — close the one live soundness hole (mutual-recursion termination).
3. **A2, A3, A4** — cheap antibody/vertical fills; A4 covers an untested kernel subsystem.
4. **#7** — automatic `rewrite` motive inference (largest ergonomic gap in equality).
5. **A8 / #22** — the term-generator metatheory engine (assurance jump), then A10.
6. **#3 → #17, #4, #5 → A24** — pattern-matching reach (compiler pass, then forms, then forcing + its vertical).
7. **#11** — postponed/suspended constraints (#10 Miller patterns landed).
8. **#14, #6, A5, A6, A9** — remaining depth/ergonomics as capacity allows.
